# SpaceFlightChess — Project Agent Guidance

## 这个项目是什么

**空间飞行棋**：PICO OS 6 Spatial SDK 应用（包名 `tech.illusion.spaceflightchess`）。桌面尺度棋盘悬浮在玩家正前方，玩家（红方）对战 3 个 AI（黄/蓝/绿）。经典完整规则：摇 6 起飞、摇 6 额外回合不封顶、踩对方飞机打回机场、自家两架飞机组成僚机免疫单机捕获（可被对方僚机反吃）、4 个飞行格触发沿自己路径前进 14 步（不递归）、回家小道需精确点数、产生第一名即结束整局并按当前完成情况排名。

全新独立项目。复用 `SpaceGomoku` 已验证的 `Stage` 容器配置、射线拾取（`CollisionComponent`+`InteractableComponent`+`detectSpatialPointerEvent`）思路；代码独立实现，不合并、不共享代码。

- 产品规划：[`../docs/superpowers/specs/2026-08-13-space-flight-chess-design.md`](../docs/superpowers/specs/2026-08-13-space-flight-chess-design.md)
- 本项目在 brainstorming 阶段之后是**用户明确授权全自主推进**（"赋予你完全自主选择能力，直到成品出来时再喊我测试"）实现的，跳过了 `spatial-design-first-build` 技能通常要求的逐元素 UI 设计契约用户确认这一步——这是有意的范围收窄，不是遗漏；后续如果要继续加新 UI/面板，仍应该按该技能的门禁走。

## 当前状态（2026-08-13）

**逻辑层完整落地并通过测试；渲染/交互层代码写完、编译通过，且已经在模拟器上真实截图确认过棋子渲染管线是通的（颜色、位置都对得上代码）。骰子手势/动画/精确摆位仍未做，是下一阶段的工作，不是本阶段遗漏。**

- `Track`/`PieceState`/`GameState`/`Move`/`BoardGeometry`/`DiceEngine`/`WinDetector`/`GameEngine`/`PlaneAi`：纯 Kotlin，**56+ 个 JUnit 单测全绿**（`./gradlew testDebugUnitTest`）。覆盖设计文档第二章全部 8 条规则的边界情况，过程中测试真正抓到并修正了两个实现 bug：
  1. 飞行格最初设计是"传送到固定绝对格"，推演后发现对某些颜色是倒退而非前进（见 `Track.applyFlight` 的类注释），改成"沿自己 path 前进固定 14 步"
  2. `PlaneAi` 最初用 `compareByDescending(...).thenByDescending(...)` 配 `maxWithOrNull`——这个组合实际选出的是最小值不是最大值，AI 会稳定选最差的招，改成 `compareBy(...).thenBy(...)` 才对
- 飞机模型资源：用户在 `~/Downloads/飞机/` 提供的 8 个 stylized `.usdz` 里选了 4 个分配给四色（见设计文档第三章的选型理由），已拷进 `app/src/main/assets/models/{red,yellow,blue,green}_plane.usdz`。PICO Spatial SDK 原生支持直接 `Entity.loadSuspend(uriString = "asset://models/xxx.usdz")`，不需要转格式（已用 `pico-spatial-knowledge` 核实：`spatial-sdk_resource-management_model.md`）
- `PlanePieceRenderer`：**每队只真实加载 1 次、其余 3 架用 `Entity.clone(cloneOptions = CloneOptions(recursive = true, shouldShareMaterialInstance = true))` 复制**——最初 16 次串行 `loadSuspend` 实测每次约 1.7s，加起来接近半分钟，真机日志实测确认后改成 4 次真加载 + 12 次 clone。运行时用 `getVisualBounds(source)` 量出每个模型的真实包围盒再算缩放/居中偏移（不是预先手动量的静态数值），统一到棋子级别尺寸——**注意 `relativeTo` 参数必须传 `source` 本身，不能传 `null`（详见下面 2026-08-14 的踩坑记录）**。棋子颜色归属现在直接靠飞机模型本体（贴图棋盘+机库颜色已经够清楚），不再挂纯色圆盘
- `BoardRenderer`：~~28 格大圈 + 4 条回家小道 + 4 个机场 + 飞行格高亮，纯静态几何~~ —— 已于 2026-08-14 全部删除，现在只剩一个贴 `board_space.jpg` 的平面（格子由贴图本身画出，见下文）
- `BoardStage`：`SpatialView` + 射线拾取选子 + `GameEngine` 事件队列驱动 UI；骰子交互目前是 `GameHud` 里的"摇骰子"按钮（点击式兜底，不是抓取甩动手势）——这是设计文档 Week 3 就定好的阶段性简化，不是遗漏
- `StartPanel`/`GameHud`/`ResultPanel`：SpatialUI + `PicoTheme`，无 Material 依赖

### 模拟器截图验证记录（9 轮，`.spatialsdk/ui-verify/`）

**结论先说**：`r3-diag.png`/`r7-clean.png`/`r9-final.png` 三轮独立截图都确认了棋子渲染管线本身是通的——颜色（`UnlitMaterial::setColor` 打的值跟代码里 `Color4` 常量完全一致）、位置（跟 `BoardGeometry` 的几何公式对得上）都对。中间踩了几个坑，按发生顺序记录，避免下一个 session 重踩：

1. **第一次装机启动报错**（`r1`/`r2` 之前）：`pico-cli app launch` 报 `ADB start activity did not create a running process`。查 logcat 发现真实原因是 `ActivityTaskManager: current full space don't allow start fullscreen`——另一个会话的应用（`tech.illusion.spaceflap`）正占着 `emulator-5554` 的 Full Space，系统拒绝了新应用的全沉浸启动请求，**跟本项目代码无关**。没有 force-stop 别人的应用，等它自己退出后重试就好了。详见 [`full-space-blocks-other-apps-launch`](../../../.claude/projects/-Users-zohar-WorkSpace-Project-PicoProjects/memory/full-space-blocks-other-apps-launch.md)
2. **`r1-onboarding.png`/`r4-fixed.png`/`r5-placement.png` 截图什么都没有**（只有模拟器默认的被动直通背景房间）：不是崩溃（`ps`/`dumpsys` 都确认进程活着、有前台焦点），是**16 个飞机模型串行加载太慢**——真机日志证实每个 `.usdz` 加载约 1.7s，16 次顺序加载要接近 30s，而截图前只等了 6-12s，场景根本没建完。改成"每队只加载 1 次 + clone 3 次"后大幅缩短
3. **棋子渲染出来了但只有一个颜色可见**（`r3-diag.png`/`r7-clean.png`，改动前是绿方）：`BoardGeometry` 把 28 格大圈和四色机场按 360° 均匀分布，这本身是对的（真实游玩时玩家会转头看不同方向），但**固定角度的截图工具只能拍到正前方那一小段**——某一色刚好落在正前方就看得到，落在侧面或者甚至相机正后方（`YELLOW` 那次）的就永远拍不到，跟代码对错无关，是"环形布局 + 静态单张截图"的天然局限
4. **加了一个角度偏移让红方（人类玩家）默认朝向观察者**（`BoardGeometry.angleOf` 减 90°）：`r9-final.png` 确认生效——红方圆盘出现在了原来绿方的位置，这是有意的产品选择：默认视角至少要能看到人类自己的机队，其余三色仍然分布在四周，靠真机转头才能看全，这是圆形棋盘的合理代价，不是缺陷
5. **同一轮尝试把棋盘半径缩小（0.30/0.40 → 0.16/0.21）导致整个棋盘从截图里完全消失**（`r8-red-facing.png`）：没有找到确切的几何原因（怀疑跟这个截图工具的固定相机俯仰角有关），**已回退到原始半径**，缩小棋盘这个方向没有正面证据支持，不要在没有真机反馈的情况下重新尝试
6. **另外单独试过把棋盘整体下移到桌面高度**（`Y` 方向 -0.40m，模拟"俯视桌面"），结果棋盘完全出画（`r4-fixed.png`），比不下移时更差——已回退为棋盘直接挂在 `rig` 上、跟头显同高，不做下移。**这个模拟器截图工具的相机应该是固定朝向、几乎不带俯角的**，跟真机里用户可以自由抬头低头完全不是一回事，不要用这个截图的构图结果去推断真机上的观感

**没做到的（不是遗漏，是下一阶段）**：骰子的抓取甩动手势（设计文档 Week 4）、飞行格起飞/降落动画（Week 4）、僚机叠放视觉细节；棋盘精确摆位（高度/距离/是否俯视）需要真机让用户实际转头看一圈才能定，这个模拟器截图工具给不了这个反馈。

**另一个更晚的会话发现了一个不同性质的坑**：截图相机拿到的有时是请求那一刻之前的陈旧帧，跟"内容本身摆得对不对判不出来"是两码事——它会让两轮 A/B 对比截图看起来一样，产出假阴性（明明变了却像没变），不只是构图判不出摆位。

## 关键文件与职责

- `app/src/main/java/tech/illusion/spaceflightchess/game/`：全部纯 Kotlin，见上方测试列表
- `app/src/main/java/tech/illusion/spaceflightchess/content/BoardStage.kt`：`mainApp` 入口经 `Main.kt` 挂到这里；`DefaultStage { PicoTheme { BoardStage() } }`
- `content/BoardRenderer.kt` / `PlanePieceRenderer.kt` / `Panels.kt`：渲染层，职责划分见设计文档第四章

## 棋盘美术贴图 + 方形坐标重构（2026-08-14）

`BoardRenderer.attachBoardArt()`：用户提供的星空主题方形飞行棋美术图（`app/src/main/assets/textures/board_space.jpg`，1254x1254）作为 `UnlitMaterial` 贴图铺在一个 `MeshResource.createPlane(0.95m, 0.95m)` 上，`pitch=-90°` 转平朝上，Y 比棋子格 tile 更低（`BOARD_ART_Y_DROP_M=0.006` vs tile 的 `0.003`）避免 z-fighting。

**`BoardGeometry` 已从圆形跑道（极坐标）改成方形回路，坐标直接量自这张图的像素**（不再是估的）：用 Python/PIL 在图上做亮度扫描+连通域检测，量出绿方（图片左上角）机库 4 个停机位圆心像素坐标、绿方归家小道 6 个格子的圆心像素坐标（在图片正中心那一行，像素间距实测 ~74.5px），换算成米（`0.95m / 1254px`），其余三色靠图片本身的四重旋转对称性，用 90°旋转公式从绿方数据推算，不是每个都单独量的。

~~一个诚实的局限：这张图只有机库、归家小道、四角转角/中心的花纹是清晰独立的圆点，共享跑道（28 格）在每条边的中段基本是空白星空背景，没有画成一格一格的独立图案，28 个共享格是插值出来的，不是量出来的。~~ **这条记录是错的**：跑道每一格都有独立印花圆点。2026-08-14 曾据此改过一版（仍按 28 格），2026-08-17 才发现印刷跑道其实是 **52 格**——最终结论见下文"棋盘其实是 52 格"一节，中间那两版都已作废。

单测 `BoardGeometryTest.kt` 已同步改掉了假设"固定半径圆环"的两个断言（改成"落点在方形贴图范围内"和"归家小道到中心的距离单调递减"），并新增一条用 `atan2` 逐格验证 28 格共享跑道确实按顺时针推进（不是假设，是算出来验证过的：green→red→yellow→blue 四个机库角的夹角、以及 28 格本身的夹角序列都严格单调，只在这个方向上转一圈）。`./gradlew testDebugUnitTest` 全绿。

## 移除自绘格子 + 棋子改用飞机模型本体（2026-08-14）

- `BoardRenderer`：删掉了整个"每格贴一个纯色圆柱 tile"的逻辑（灰色跑道格、橙色飞行格、四色归家小道/机库格全删），现在只剩 `attachBoardArt()` 这一个贴图平面。原因：贴图本身已经画好了所有格子，叠加自绘圆柱只会重复且视觉打架。
- `PlanePieceRenderer`：删掉了棋子底下那个"纯色圆盘"（原本用来弥补飞机模型涂装对不上队伍颜色的问题）。拾取（`InteractableComponent`/`CollisionComponent`）现在直接挂在 `pivot` 上（一个跟飞机同尺寸的隐形碰撞盒），不再依赖圆盘承载——`BoardStage` 的射线拾取链路（`pieceFor` 查表）不用动，因为它本来就是"查表映射被命中的实体"，只是把被查的实体从圆盘换成了 pivot。

**装机截图当时发现红方、绿方机库看不到飞机，一开始怀疑是材质金属度在模拟器光照下渲染成黑——这个猜测是错的，见下面 `getVisualBounds` 踩坑记录，真实原因是坐标算错了不是材质问题。**

## 棋子坐标越界 + AI 摇 6 后卡死（2026-08-14，真机反馈驱动）

用户反馈"飞机有些掉到棋盘下面、有些嵌入棋盘内"，以及"摇到 6 之后不管 AI 还是玩家都不再有机会摇了，游戏卡死"——两个独立 bug，都是当天用真机截图/描述倒逼出来的，不是模拟器能自己发现的。

**Bug 1：`PlanePieceRenderer.attachTo` 里 `getVisualBounds(null)` 的 `relativeTo` 参数传错了。**
- `getVisualBounds(relativeTo, recursive, enabledOnly)` 的 `relativeTo` 文档原话："Set to null to indicate `SpatialContainer`"——传 `null` 量出来的包围盒是**相对整个 Stage/世界坐标**，不是模型自己的局部坐标。而 `source` 在测量前已经被挂到 `probePivot`→`rig`（`rig` 在 `BoardStage` 里被摆到世界 Y=`RIG_HEIGHT_M`≈1.15m），所以量出来的 `bounds.center.y` 里混进了整条祖先链的偏移，主要是 rig 的 1.15m 高度。
- 后续 `recenter = -bounds.center * scale` 就把这个 1.15m 也按每个队伍自己的缩放系数放大减掉了。红方 `scale≈0.171`、绿方 `scale≈0.332`（这两个 `.usdz` 原始模型很小，缩放系数大），1.15m 的误差被放大成 20-38cm 的真实下坠，肉眼就是"掉到棋盘下面/地板上"；黄方、蓝方 `scale≈0.016`（原始模型接近真机比例的大飞机），同样的 1.15m 误差缩到只有 1-2cm，肉眼只是"贴得有点近/略嵌入"——这就是为什么用户观察到"有些掉下去、有些嵌进去"两种不同表现，其实是同一个 bug 在四个缩放系数差异极大的模型上的不同放大倍数。
- **修法**：`relativeTo` 改传 `source` 自己（`source.getVisualBounds(source)`），量出模型真正的局部包围盒。用临时 log 逐队打印 `boundsCenter`/`recenter` 验证过：修复前四队 `boundsCenter.y` 分别是 1.298/2.029/1.054/1.148（都混进了 ~1.15 的 rig 高度），修复后变成 0.148/0.879/-0.096/-0.002（干净的局部值）。装机截图（`.spatialsdk/ui-verify/clean-final.png`）确认四色机库棋子都正确贴在棋盘对应格子上，无下坠无嵌入。
- **排查这个 bug 时的一个教训**：中途有一次装机截图（`bounds-fixed-2.png`）看到红方飞机飘在卧室书架旁边，一度怀疑修复不完整——后来查 `dumpsys activity activities` 发现 `mFocusedApp` 其实是另一个项目的 `tech.illusion.spaceplayer`，因为本项目的 device-lock（`verify-bounds-fix`，ttl=150s）在冗长的调试过程中提前过期，被另一个并发 session 合法抢占了共享模拟器 `emulator-5554`。**结论：那次截图看到的根本不是本项目的内容**，是锁过期导致的假阳性。以后长时间反复装机测试要么把 ttl 设更大（这次改用了 300s 起步)，要么定期用 `bash .claude/skills/spatial-design-first-build/scripts/device-lock.sh acquire <owner> <ttl> emulator-5554` 续期，并且每次截图前先用 `adb shell dumpsys activity activities | grep mFocusedApp` 确认焦点确实是 `tech.illusion.spaceflightchess`，不要假设锁还在手上。

**Bug 2：`BoardStage.kt` 里驱动 AI 回合的 `LaunchedEffect(phase, currentTeam)` 键选错了。**
- 摇到 6（`Track.LAUNCH_ROLL`）时，`GameEngine.resolveEndOfRollSegment` 的设计是"同一个队伍再摇一次"——`phase` 变回 `AWAITING_ROLL`，`currentTeam` **保持不变**。也就是说，AI 某队摇 6 拿到额外回合后，`(phase, currentTeam)` 这个二元组和它*上一次*触发这个 `LaunchedEffect` 时的值**完全一样**。Compose 的 `LaunchedEffect(keys...)` 只在 key 的值相对上一次变化时才会重启协程体——键值没变，效果就不会重新执行，AI 那个"再摇一次"的自动摇骰子永远不会发生，整局游戏卡在这个 AI 的回合上，谁都摇不了。
- 人类（红方）这边理论上手动再点一次"摇骰子"按钮应该还能摇（按钮的 `enabled`/`onClick` 没有因为 key 没变就失效），但一旦任意一个 AI 卡死，后续所有人的回合都排不到——所以用户描述的"无论 AI 还是玩家都卡住"，本质是同一个 AI 侧 bug 造成的全局卡死，不是人类侧也有独立的 bug。
- **修法**：改成 `LaunchedEffect(revision)`——`revision` 是 `publish()` 里每次都无条件 `revision++` 的计数器，保证每次状态变化后这个 key 一定是全新值，不管 `phase`/`currentTeam` 是否恰好绕回了原值，都会正确重新触发。
- **这个 bug 没能在模拟器上交互验证**：模拟器的 `pico-cli capture`/`adb shell input tap` 打不到这种体积式空间 UI 的按钮/棋子（`spatial-design-first-build` 技能原话），没法真的摇一次 6 看 AI 是否继续。只能确认代码逻辑（`resolveEndOfRollSegment` 保持同队伍 + Compose key 语义）自洽，**需要用户在真机上实际摇到一次 6 验证 AI/玩家都能连续摇下去**。

## 选阵营 + 棋盘随座位旋转 + 头显下方摆放（2026-08-14）

新增：开局面板（`StartPanel`）从"固定红方"改成 4 色圆形阵营选择器（`FactionSwatch`），点"开始游戏"才真正 `engine.startGame()`；`BoardGeometry.configureSeat(team)` 让整块棋盘（贴图 + 全部 16 个棋子）额外旋转，把选中阵营的机库转到"贴近玩家"的固定象限（`SEAT_TARGET_STEPS`=2，即黄方原始象限，棋盘右下角）；`BoardStage` 的 rig 改成跟随 `HMDTrackingProvider` 的真实头部位置（`pose.position.y - BOARD_HEIGHT_BELOW_HEAD_M`），而不是固定摆在眼前——固定摆法会让人感觉"卡在棋盘中间"（近边只有 ~7.5cm）。HMD 采样有 1 秒超时兜底（模拟器基本采不到数据，真机才有）。

**Bug：贴图旋转方向和棋子旋转方向反了。** `BoardGeometry.rotate()`/棋子位置用的是"自己的顺时针"约定（单测 `the shared loop advances clockwise...` 验证过），但 `BoardRenderer.updateSeatRotation` 一开始直接把同一个角度值塞进 `EulerAngles(pitch=-90, yaw=seatRotationDegrees)` 的 `yaw`——结果棋子转对了，贴图却往反方向转了一步。真机截图（用户提供）和模拟器截图交叉验证一致：选红方后，红方飞机确实转到了"座位象限"，但那个象限印的还是蓝方原始机库图案，没跟着转。根因是 `pitch=-90` 已经把这个 plane 的本地 Y 轴掰离了世界竖直方向，此时再叠加的 `yaw` 不再是"绕世界竖直轴单纯自转"，两个旋转合成后手性和 `BoardGeometry` 自己的约定对不上（经典 Euler 顺序陷阱，没有去查 `Quat` 乘法顺序就先试了取反）。**修法**：在 `BoardRenderer.updateSeatRotation` 里把传入的角度取反（`yaw = -yawDegrees`），不改 `BoardGeometry` 自己的符号（棋子那边的单测已经锁定了那个符号）。装机截图验证：红方座位下，左上=蓝、右上=绿、右下=红（红机+红印花重合）、左下=黄，四色贴图和棋子完全对应。

**新增：选阵营时的旋转/移动动画。** 之前是整块贴图 + 16 个棋子瞬间跳到新位置（用户反馈"直接把红色飞机移到了蓝色机库"，观感像瞬移到错的地方，而非棋盘转向自己）。现在 `startWithFaction` 在调用 `configureSeat` 前后各采样一次全部 16 个机库槽位坐标（`fromHangars`/`toHangars`）和棋盘朝向角（`fromYaw`/`toYaw`），用 30 帧 * 16ms（约 480ms）的协程循环、`smoothStep` 缓动，把贴图朝向（`boardRenderer.updateSeatRotation`）和每个棋子的枢轴位置（新增 `PlanePieceRenderer.setPivotPosition`，绕过 `GameState` 直接摆位置，只给这个动画用）逐帧插值过去，角度插值走最短路径（`lerpAngleDegrees`，避免 270°→0° 转 270 度那种绕远路）。动画结束后照常 `engine.startGame(); publish()`，`render()` 会用真实 `GameState` 把位置钉死一次（含 jitter 偏移），动画本身没做 jitter，误差在 1.2cm 内、动画末尾几乎无感。

这个动画只覆盖"选座位"这一次性过渡；棋子后续在跑道上的正常移动仍然是瞬移（design doc Week 4 的起飞/降落动画还没做，见下面"下一步"）。

## 骰子模型替换按钮摇骰（2026-08-14）

用户提供了一个真实骰子模型（`Wooden_Rounded-edge_Die.usdz`，来自 Sketchfab 风格导出，单一 PBR 材质：baseColor+metallic+roughness+normal 四张贴图），要求用它替换 HUD 面板里"摇骰子"按钮那种平面交互方式，改成点这个骰子本体来摇，摇的过程随机翻滚，最后停在对应点数朝上。

**贴图和点数的映射是实测的，不是猜的。** 这个 `.usdz` 的点数是烘焙在一张贴图上的，没有任何可读的 per-face 元数据，装机截图里点数怎么摆完全取决于原始建模者自己的习惯，不能假设"1 朝上、2 朝前、3 朝右"这种教科书摆法。做法：把骰子按 3 种已知的单轴旋转（原始朝向 / `pitch=90` / `roll=90`）摆到模拟器里，截图直接读朝上那面的点数，另外 3 面用"对面之和等于 7"这条任何骰子都成立的规律反推出来（不用再截 3 次）。实测结果：

| 旋转 | 朝上点数 |
| --- | --- |
| 原始朝向（无旋转） | 5 |
| `pitch=90°` | 1 |
| `pitch=-90°` | 6（= 7-1，pitch90 的对面） |
| `pitch=180°` | 2（= 7-5，原始朝向的对面） |
| `roll=90°` | 3 |
| `roll=-90°` | 4（= 7-3，roll90 的对面） |

这张表写死在 `DieRenderer.VALUE_TO_ROTATION` 里，`yaw` 全程不用（`yaw` 绕垂直轴转不会改变哪面朝上，只会转朝向），避开了 `EulerAngles` 多轴合成顺序不直观的坑（同一次会话早前 `BoardRenderer` 的贴图旋转 bug 就是叠加 `pitch`+`yaw` 两个非零分量栽的跟头，这次全程只用单一非零分量，规避了合成顺序问题）。

**架构**：`DieRenderer`（新文件）复用 `PlanePieceRenderer` 那套"`getVisualBounds(self)` 测量本地包围盒 → 缩放到目标尺寸 → 用包围盒中心做 recenter"的手法（同样的坑：`relativeTo` 必须传自己，不能传 `null`）。`isDie(entity)` 供 `BoardStage` 的 `detectSpatialPointerEvent` 识别点击目标；`tumbleTo(value)` 是 suspend 函数，先在 4 个随机 waypoint 间连续插值转动（每个 waypoint 用 `AnimationMath.lerpAngleDegrees`/`smoothStep`，和 `BoardStage` 的选座动画共用同一个 `AnimationMath.kt`），最后缓动定格到目标点数的姿态。`BoardStage.rollAndAdvance()` 改成 suspend：`engine.roll()` 拿到点数后先等骰子翻滚完再让 AI 应用走法/再 `publish()`，`isRolling` 标志防止翻滚过程中重复触发。`GameHud` 的"摇骰子"按钮整个删掉，改成一行提示文字（"点击骰子摇一摇"），触发摇骰完全靠点骰子本体。

骰子摆在棋盘外侧、正对玩家座位的角落（`DIE_POSITION = (0.50, 0.025, 0.50)`，相对 rig 局部坐标——`BoardGeometry` 把选中阵营的机库转到 `+X/+Z` 象限，这个位置正好在那个象限之外，不会盖住任何机库停机位或跑道格）。

**装机验证情况**：
- ✅ 骰子的缩放/摆位/点数-旋转映射：通过临时的 3 骰子并排校准截图（关闭面板遮挡后）直接验证过，三个朝向读出来的点数分别是 5/1/3，和上表一致。
- ✅ 代码编译通过、`testDebugUnitTest` 全绿、装机启动无 crash（`adb logcat -b crash` 干净，`Log.e` 的 `scene ready` 正常打印）。
- ❌ **完整的"点骰子 → 翻滚 → 停在真实摇到的点数 → HUD 联动"这条链路，这次会话没能在模拟器上截图验证成功**——不是代码逻辑的问题，是撞上了下面这条新发现的、和骰子本身无关的环境问题。

## 发现一个独立的渲染问题：面板一旦挂上，3D 内容整个不出画（2026-08-14，未解决）

在这次骰子验证过程中反复装机截图时发现：只要 `PANEL_IDS`（start/hud/result 三个 `AttachmentPanel`)被挂到 `rig` 上，摆脱不了两种坏结果之一——要么 SETUP 阶段的 `StartPanel` 整个填满画面（明显不是原本 360dp 应有的尺寸，具体原因没查，可能是这批面板自 HMD-跟随重构后就没在非 SETUP 阶段真机验证过尺寸），要么进入 `AWAITING_ROLL`/`AWAITING_MOVE` 后画面直接是空房间（棋盘、棋子、骰子全部不可见），只剩背景环境本身在渲染。**用一个对照实验排除了骰子本身的嫌疑**：把 `dieRenderer.attachTo(rig)` 整行注释掉、只留棋盘+棋子+三个面板，问题原样复现；反过来，早前"3 个骰子校准"截图里完全不挂三个面板时，棋盘外的骰子渲染完全正常。也用 `pico-cli app stop` 之后直接截图确认过：截图工具本身没问题（能拍到停止应用后露出的真实背景房间），说明不是"截图缓存/卡死"，是应用挂着面板时真的没有把 3D 内容合成进最终画面。

排查过程中还发现这次会话高频重复装机启动了几十次，中途一次 `pico-cli app launch` 甚至没能拉起进程（重试一次才成功）——不排除是这个共享模拟器长时间高强度使用后本身状态劣化，但即使换了全新的 fresh app 进程、fresh PID，问题依然 100% 复现，说明至少这个"面板+3D 内容"的现象本身是可复现的，不能全部归因于模拟器疲劳。**这个问题没有解决，也没找到根因**，记录在这里供下次继续排查：优先怀疑每个 `AttachmentPanel` 是否各自占用一个独立的 compositor Surface/Window（真机日志里能看到多个 `Window{...LaunchActivity}` 记录），3 个面板 + 较重的 3D 场景（4 个飞机模型 + 1 个 4 张贴图的骰子模型）是否撞到了某种 surface/资源上限。**建议下一步：先用全新模拟器实例（或真机）复现一遍，排除"当前模拟器已用坏"这个可能性，再决定要不要专项修这个面板渲染 bug。**

## ~~共享跑道 28 格坐标重新实测~~（2026-08-14）——**整节已被 2026-08-17 的 52 格重构取代，见下文**

**⚠️ 这一节测对了"格子在哪"（跑道沿外围八边形、每格独立印花），但当时没检查"哪个绝对格编号对应哪个格子"——后者的 bug（起飞格算到了错误一侧）一直存活到 2026-08-17 才被用户的参照图揪出来，见下面"起飞格坐标算反了"一节。**

用户直接问"你当前是将棋路按照棋格进行正确分割"，倒逼重新核对了 `BoardGeometry.ringPosition`——结果发现上面"棋盘美术贴图 + 方形坐标重构"一节里"共享跑道没有独立印花、只能插值"的判断是**错的**。用 Python/PIL 对 `board_space.jpg` 做了更彻底的连通域 blob 检测（阈值放宽到 `bright>140 and sat<55`，此前太严格漏检了不少格子），发现整条 28 格跑道其实是沿棋盘外围的八边形走的，每一格都是独立印花圆点，而归家小道是棋盘中心十字臂上的另一条完全独立的路径（各队自己的颜色，6 格）——这两条路径此前被我搞混了：旧代码把跑道格摆在"归家小道同款间距、紧挨着归家小道平行"的位置，实际上离真正的印花跑道（外围八边形）差了一整个棋盘半径，跑道格坐标基本没对上任何真实格子。

**外围八边形每条边（对应 `Track.LOOP_SIZE/4=7` 格一段）的形状是"对角切角 + 5 个直排格 + 对角切角"，不是一条直线**——用绿方自己的一段（絕对格 21-27，紧邻绿方机库、一路走到紧邻红方机库为止，是 `quadrantSteps(GREEN)=0` 对应的"零旋转参照段"）逐格量出的像素坐标：

| 段内序号（`localIndex`） | 绝对格 | 像素坐标 | 备注 |
| --- | --- | --- | --- |
| 0 | 21 | (387.9, 118.2) | 紧邻绿方机库的对角切角格 |
| 1 | 22 | (468.3, 87.3) | |
| 2 | 23 | (547.2, 87.2) | |
| 3 | 24 | (627.8, 87.9) | 顶边正中——`Track.flightCells` 里绿方这一段的飞行格 |
| 4 | 25 | (709.8, 87.3) | |
| 5 | 26 | (787.5, 87.3) | |
| 6 | 27 | (869.6, 119.3) | 紧邻红方机库的对角切角格 |

这 7 个点写死进 `BoardGeometry.GREEN_LOOP_PX`（一个显式坐标列表，不再是"起点+等距间距"的公式），`ringPosition` 直接按 `localIndex` 查表、再套用原有的 90° 旋转（其余三队仍然靠图片的四重旋转对称性从这 7 个点推算，旋转逻辑本身没有问题，问题只在参照点测错了）。归家小道那一段的坐标（`GREEN_HOME_ROW_PX`/`PITCH_PX` 公式）实测下来跟原有代码已经很接近（几个像素内，可以忽略），没有动。

**怎么验证的**：给整张图叠加 100px 网格截图，肉眼+像素坐标交叉核对；用连通域检测把所有印花圆点的质心坐标批量导出，画到图上核对哪些被漏检/误检；专门放大截图核对了"对角切角"格到底是一个格子还是两个格子共用一个方形（结论：是两个相邻格各占对角一半，不是一个双圆点的特殊格）。`BoardGeometryTest.kt` 的顺时针单测（`atan2` 逐格验证）用新坐标手算过一遍确认仍然单调递增，`./gradlew testDebugUnitTest` 全绿。

**没能做到的**：这次会话没能在模拟器上截图确认棋子现在真的落在这段新测的印花格子上——尝试了一个不带面板的临时验证 build（棋子摆到绝对格 21-24 附近），但撞上了上面"面板一旦挂上 3D 内容就不出画"那条记录的同一个环境问题，而且这次连不挂面板都拍不出内容了（截图仍是空房间），说明这个共享模拟器的渲染管线这次会话内进一步劣化了，不只是"挂面板才坏"。**这个坐标修复本身的正确性目前只有像素测量+单测两层证据，还没有真机/新鲜模拟器的截图佐证，是下一步真机验证时的第一优先级检查项**（用户戴上头显应该能直接看到棋子摆在跑道印花圆点上而不是漂在旁边的空白星空背景里）。

## 棋子移动动画（2026-08-15，真机反馈"摇到6选择飞机后直接飞到第8格"排查）

用户真机反馈：红方摇到 6、选择机库里一架飞机，结果那架飞机直接飞到了"第8格"；并明确指出预期行为——摇到 6 选择机库飞机应该先到"待飞位"（也就是 `path=0`）停住，要再摇一次、再选一次那架飞机，才应该按新摇到的点数继续飞。

排查结论：**这不是逻辑 bug**。设计文档第130行原文"`path`（0=起飞...）"、`GameEngine.kt` 里 `PieceState.InHangar -> if (roll == Track.LAUNCH_ROLL) moves += Move(team, index, piece, PieceState.OnPath(0))`——起飞永远落在 `path=0`，且摇到 6 会走 `resolveEndOfRollSegment` 的额外回合分支（同一家继续 `AWAITING_ROLL`），不会自动帮玩家再摇一次或再选一次。也就是说代码本来就是"起飞停在 0 号位、需要玩家自己再摇一次骰子、再点一次这架飞机才会继续走"这个两步流程，跟用户描述的预期完全一致。

真正的问题是**这两步之间完全没有过渡动画**——`PlanePieceRenderer.render()`/`setPivotPosition` 都是瞬间 `setPosition`，起飞落地到 `path=0` 和后续再摇一次点一次移动到新格子，视觉上都是"啪"地瞬移。用户很可能是很快点了骰子+飞机两次，中间那次"停在待飞位"的瞬移因为没有任何动画反馈，一闪而过没被注意到，两次瞬移看起来就像"选一次直接飞好远"。

**修法**：`PlanePieceRenderer` 新增 `suspend fun animateMove(team, index, state)`——读棋子 pivot 当前的实际渲染位置作为起点，`BoardGeometry.positionFor` 算出的新位置作为终点，24 帧（~384ms）`smoothStep` 缓动滑过去（复用 `AnimationMath.kt` 的 `lerp`/`smoothStep`，跟选阵营动画、骰子翻滚动画同一套帮手函数），而不是瞬移。`BoardStage.kt` 两处调用点都改了：人类点棋子移动（`detectSpatialPointerEvent` 里）、AI 摇完骰子后选走法（`rollAndAdvance` 里）都在 `engine.applyMove(move)` 之后先 `animateMove` 该棋子，动画放完再 `publish()` 刷新 HUD/其余棋子——起飞（机库→跑道 0 号格）和后续每一步推进现在都会走同一条滑动动画，不再是瞬移，两次移动之间应该能看清"停在这一格"再"继续飞"。

**没做的**：被吃回机库的棋子（`resolveCellEffects` 里 `withPiece(otherTeam, idx, PieceState.InHangar)`）目前还是瞬间传送回机库，没有动画——用户这次反馈的场景是自己主动移动的棋子，没提到打回机场的动画，所以先按最小范围只做了"移动中的这颗棋子"，打回机场的动画留到以后有人提再做。

**装机验证情况**：build+`testDebugUnitTest` 全绿，装机启动无 crash（`scene ready, phase=SETUP` 正常打印）。动画本身是"一段时间内的视觉过程"，静态截图看不出效果，模拟器又打不了体积式 UI 的交互手势，所以这次会话没能截图/录屏确认动画真的在放——这条和上面"共享跑道坐标重测"一样，需要用户在真机上实际摇一次骰子、走一步棋，直接看效果。

**⚠️ 更正（2026-08-17）：上面"排查结论：这不是逻辑 bug"这句判断错了，但不是因为这个具体排查过程有问题——是因为排查时用来对照的"起飞格应该在哪"这个参照系本身就是错的。真正的 bug 记录在下面"起飞格坐标算反了"一节：起飞格确实贴着自己机库，但贴在错误的一侧（朝向"下一家"而不是朝向"上一家"），所以用户看到的"飞机落点比预期远一截"是真实存在的坐标偏移，不是本节说的"动画缺失导致的错觉"。本节修的动画缺失问题依然是真实且值得修的（动画本身没错），但当时用来验证"落点对不对"的参照系是错的，导致误判了整个反馈的性质。

## 摇骰/走子调试日志（2026-08-15）

用户用截图反馈"摇到6选择飞机后走到第8格"的问题时，说清楚是模拟器（只是切换了背景环境，不是真机）之后，让顺手加上日志方便下次同类问题直接查日志排查，不用来回猜。`BoardStage.kt` 现在在关键决策点都打了 `Log.e(TAG, ...)`（沿用这个文件已有的习惯——`scene ready`/`no HMD pose` 这些非报错信息也用 `.e`，所以复用同一个 `adb logcat -s SpaceFlightChess:E` 过滤器就能看到全部）：

- `roll: team=... value=... phaseAfter=... currentTeamAfter=...` —— 每次 `engine.roll()` 之后打，不管是人类还是 AI。
- `move[human]: ...` / `move[ai]: ...` —— 每次 `engine.applyMove(move)` 之后打，包含 `from`/`to`（完整 `PieceState`）、`absCell`（如果落在共享跑道上，直接算出绝对格方便对照贴图印花）、`outcome`（`MoveOutcome`，含吃子/僚机/飞行/进小道/到终点）、动作后的 `phase`/`currentTeam`。
- `die tap ignored: ...` —— 点了骰子但条件不满足（不是 `AWAITING_ROLL`、不是人类回合、或者 `isRolling` 还没复位）时打，能看出是不是有重复触发。
- `piece tap ignored: ...` —— 点了棋子但不是 `AWAITING_MOVE` 或不是自己回合时打。
- `piece tap: no legal move for team=... pieceIndex=... legalMoves=...` —— 点中了棋子但这颗棋子当前没有合法走法（比如点了机库里的飞机但这次没摇到 6）时打，附带打印当前全部合法走法方便对照。

**下次遇到"棋子好像走错/走远了"这类反馈，直接让用户描述前后操作，然后 `adb logcat -d -s SpaceFlightChess:E`（或先 `adb logcat -c` 清一次再重现）把 `roll`/`move` 这几行摘出来**，`from`/`to`/`absCell` 摆在一起基本就能看出这一步棋是不是按预期的 `path` 数值走的，不用再靠截图和用户口头描述互相猜。`GameEngine.kt`/`Track.kt` 这些纯 Kotlin 游戏逻辑层本身故意不依赖 Android（这样才能脱离设备跑单测），所以日志只打在 `BoardStage.kt` 这个 Compose/Android 层的调用点上，没有污染纯逻辑层。

## ~~起飞格坐标算反了：贴着机库的哪一侧~~（2026-08-17 上午）——**这次修复只是把 28 格模型里的错挪了个位置，已被下文 52 格重构取代**

用户反复反馈"摇到 6 之后飞机落点不对"，我起初两次都用截图肉眼核对"贴不贴着机库"就下了"没问题"的结论——这是不够的：两个诊断都只验证了"贴着机库"，没验证"贴在机库的哪一侧"，而这次真正的 bug 恰好就是侧搞反了。

**用户拿出了一张自己标注的参照图**（`img17_full.jpeg`，四个机库的"待飞区"都用白圈标了出来），明确显示：**每个机库的待飞区，贴在"上一家"（顺时针方向的前一个队伍）那一侧，不是贴在"下一家"那一侧**。例如红方的待飞区贴着绿方那一侧（绿方在红方的"上一家"，因为顺时针顺序是绿→红→黄→蓝），而不是贴着黄方那一侧。

**根因**：`BoardGeometry.GREEN_LOOP_PX` 是沿绿方机库到红方机库这一条顶边实测的 7 个点（下标 0 贴绿方、下标 6 贴红方）。旧版 `ringPosition` 把 `absoluteCell % 7 == 0`（每一家自己的起飞格）算成"下标 0 rotate 一次"，这实际上把"贴着绿方的那个点"转到了"贴着下一家（黄方）"的位置，而不是"贴着自己机库、朝上一家（绿方）那侧"的位置——相当于把每一家的起飞格都算到了自己机库正确位置往前一整个机库的地方。用像素坐标重新推导+数值验证（不是重新肉眼截图猜）：红方起飞格应该复用下标 6（"贴红方"那个实测点），旋转 0 次（不需要额外旋转，因为这个点本来就实测在红方机库正确的那一侧），而不是旧代码里"下标 0 旋转 1 次"。用 Python 重新算了全部 28 个绝对格的坐标，验证：①四色的起飞格分别贴着自己机库、且方向都一致朝向"上一家"（用 4 个队伍的实测距离数字交叉验证，不只验证了红方一家）；②28 个点仍然满足顺时针单调（复用原有单测的验证方法）；③归家小道入口格（27 号，绕完一整圈回自家前一格）在新坐标下确实紧邻新算出的 0 号格，路径连续不跳格。

**连带修复**：飞行格（`Track.flightCells`）原来是 `{3,10,17,24}`，对应贴图上每条边正中间那个"星标飞行格"图案；起飞格坐标位移一格之后，这个图案现在对应的绝对格编号也整体 +1，改成了 `{4,11,18,25}`（`FLIGHT_ADVANCE=14` 配对关系不变：4↔18、11↔25）。相应更新了 `TrackTest.kt`、`GameEngineTest.kt` 里所有硬编码引用旧编号 3/10/17/24 的用例（改成 4/11/18/25，逻辑断言本身不变）。

**改了哪些文件**：`BoardGeometry.kt`（`ringPosition` 重写，`localIndex==0` 单独分支复用下标 6 而非下标 0；class doc、`GREEN_LOOP_PX` 注释同步更正）、`Track.kt`（`flightCells` 常量 + 相关文档注释）、`TrackTest.kt`、`GameEngineTest.kt`（硬编码的绝对格/path 数值同步平移）。`./gradlew testDebugUnitTest` 全绿，`assembleDebug` 通过。

**验证情况（诚实记录）**：这次的证据链比之前扎实很多——不是"截图看着像对"，而是：①用户自己的参照图给出了 2 个独立机库（红、绿）的确认点；②我用 Python 重新做了一遍完整的像素测量 + 旋转矩阵数值计算（不是目测），四个队伍的起飞格-自家机库距离、起飞格-上一家机库距离都算出来交叉验证过，模式在四个队伍上完全一致，不是凑巧对上红方一家；③28 格顺时针单调性、归家入口连续性都重新验证过。**但这次会话结束前没能截图确认到真机/模拟器画面里棋子真的落在新算出的格子上**——尝试了一次装机截图，模拟器相机这次的默认朝向没有对着棋盘（拍到的是背景卧室的花瓶和树枝装饰，不是棋盘所在的方向），换了一次角度还是同一个画面，没法在这次会话内解决相机取景问题。这是**这次会话第三次**在这个具体环节被模拟器环境问题卡住（前两次分别是"面板一挂就不出画"和"截图工具拍到别的房间"）——数值验证已经做得很彻底了，但视觉实拍确认这一步，接下来必须靠用户在真机上直接看效果来补上。

## 棋盘从"正下方"改成"前方偏移"，不再把玩家摆在棋盘正中间（2026-08-17，真机反馈）

用户真机反馈："启动游戏应用后用户显示在棋盘中央，不利于整体查看"。

这是"选阵营 + 棋盘随座位旋转 + 头显下方摆放"一节那次改动（`rig` 位置跟随 HMD 的 x/z，只在 y 方向减 60cm）带来的新问题：`rig` 的水平位置直接等于头显的水平位置，而棋盘贴图/棋子/骰子全部是 `rig` 的子节点、以 `rig` 原点为中心摆放——结果棋盘中心正好落在玩家双脚正下方，玩家等于站在一整块 0.95m 见方棋盘的正中央，低头只能看到棋盘中间一小圈，边缘和四个机库都被自己的身体挡住或需要低头到不自然的角度才看得到。这跟这一步最早想解决的"卡在棋盘中间"是同一类问题换了个方式复发——上次是"贴脸悬浮"，这次是"脚下踩着棋盘中心"，都不对。

**修法**：`rig` 的水平位置现在是"头显位置 + 朝向 x 偏移量"，而不是直接等于头显位置——新增 `BOARD_FORWARD_OFFSET_M=0.55f`，方向取自 `HMDPose.rotation`（一个 `Quat`，之前只用了 `position` 没用过 `rotation`）在场景刚启动那一刻的朝向，投影到水平面并归一化（新增 `horizontalForward(rotation: Quat): Vector3`，用 `Quat.rotateVector(Vector3.FORWARD)` 拿到朝向向量、砍掉 y 分量再归一化；如果拍扁后长度太短——比如刚好抬头/低头到接近垂直——退回 `Vector3.FORWARD` 兜底，避免除零/归一化异常）。效果是棋盘中心从"贴脚下"挪到"眼前方向偏移 0.55m"，让玩家站在棋盘的近边缘而不是正中央，观感上更像"站在一张桌子跟前"而不是"踩在桌面中间"。0.55m 这个距离是参照棋盘中心到角的半对角线（`0.95m` 见方棋盘，半对角线 ≈0.67m）估的，让近角基本落在脚边、远角还在伸手可及范围内，不是精确量出来的，可能需要真机试戴后微调。

没有朝向数据时的兜底摆位（HMD 首帧数据到达前的 `FALLBACK` 分支，以及模拟器上 HMD 数据超时后维持的那个默认姿势）现在也用同一个 `Vector3.FORWARD * BOARD_FORWARD_OFFSET_M` 做偏移，不再是原点不偏移——这个方向本身是任意选的（没有"朝向"数据可用时没有别的依据），但至少跟有朝向数据时的行为保持"往前偏移一段距离"这个统一效果，不会退化回"摆在正下方"。

**没有改**：三个面板（`PANEL_OFFSETS`）目前还是 `rig` 局部坐标系里固定的偏移量，不跟随头显朝向单独再转一次——`rig` 本身也从来没有被设置过旋转（只 `setPosition`，没调用过 `setQuaternion`/`setEulerAngles`），所以这个"面板固定偏移是否跟玩家朝向对得上"这件事其实从最早加面板开始就没解决过，这次只是没有让它变得更糟，不是新引入的问题，仍然标记在"下一步"里等真机确认。

**装机验证情况**：build + `testDebugUnitTest` 全绿。这次直接装到了用户的真机（设备 ID `PB311XKGL4160087B`，通过 USB 连接的实体 PICO 一体机，不是共享模拟器，所以不需要走 `device-lock.sh` 那套排队协议）确认过启动无 crash、日志里没出现"no HMD pose within...”的兜底提示（说明真机上确实拿到了真实的 HMD 位置+朝向数据，走的是精确朝向那条分支，不是兜底分支）。但**这次没能亲眼确认摆位是否真的舒服**——`pico-cli capture screenshot` 在这台真机上报错（`ADB screencap returned non-PNG output`，跟模拟器不是同一套截图机制，没深入排查原因），没法截图看效果，只能确认代码跑起来没崩。摆位是否真的解决了"卡在中间"的观感问题，需要用户自己戴上头显直接感受。

**⚠️ 更正（同一天，紧接着的真机反馈）：方向反了。** 用户装机戴上后反馈"棋盘布置到用户后方了"——`horizontalForward` 算出来的方向，跟真机上头显实际朝向的**反方向**一致，不是同一方向。也就是说 `rotation.rotateVector(Vector3.FORWARD)` 这个调用本身没有报错、返回值看起来是一个合理的单位向量，但它代表的物理朝向跟这个 SDK/这台设备实际使用的"前方"约定对不上——这跟本项目之前踩过的"贴图旋转方向和棋子旋转方向反了"（见"选阵营+棋盘随座位旋转"一节）是同一类坑：没有先去查阅 `Quat`/`HMDPose` 旋转约定的文档或做最小实验，就假设了一个"看起来合理"的调用方式，结果符号/朝向假设是反的，只能靠真机实测才发现。**修法**：直接在 `horizontalForward` 里把算出来的水平分量取反（`Vector3(-forward.x, 0f, -forward.z)`），不改 `BOARD_FORWARD_OFFSET_M` 本身的语义（调用方仍然只需要知道"这是一段往前的距离"）。已重新 build + 装到同一台真机确认启动无 crash；**朝向是否真的翻正了，这次同样没能截图验证**（这台真机的 `pico-cli capture screenshot` 一直报错，见上面记录），还是要靠用户亲眼确认。**如果这次修完还是不对（比如变成了摆在侧面），下一步别再猜符号，去查 `com.pico.spatial.core.math.md` 或 `pico-dev-knowledge` 里 `Quat`/`HMDPose` 的旋转约定文档，或者从 `pose.rotation` 打印出实际数值、在几个已知朝向下核对一遍，不要再靠"反一次试试"来回猜。**

## 棋盘其实是 52 格：28 格模型是全部对齐问题的总根因（2026-08-17，四路审计后重构）

用户报了两个症状："摇到6后起飞到了第一格而不是待飞区"、"再次摇骰子后会累加之前的6格一起走"。我先后两次把它诊断成"不是逻辑 bug"（一次归因于动画缺失、一次归因于起飞格贴错了哪一侧），两次都错。第三次改用 4 路并行审计 + 每条发现独立对抗验证（36 个 agent，21 条确认缺陷 / 11 条被推翻）才挖到总根因。

**根因：印刷棋盘外圈是 52 格，代码写的是 `LOOP_SIZE = 28`。** 52 不是 28 的倍数，每队印刷间隔是 13 不是 7，**任何旋转/缩放都不可能让 28 个抽象格对齐 52 个印刷圆点**——之前所有"对齐修复"都是在一个不可能成立的前提下挪位置。

**测量方法（可复现，我独立复现过审计的结论）**：对 `board_space.jpg` 全图做连通域检测拿到 76 个 ≥500px 的奶白色圆点，剔除 4 个非格子伪影（机库边框辉光 1856px、飞行航线箭头 607px、两个画外杂点），再以 95px 阈值建互近邻图。图恰好裂成**一个 52 节点环**（外圈）+ 四条 6 格归家小道——因为环内相邻步距是 67-89px，而环到归家小道口的间距是 108px，阈值刚好把两者分开。沿环走一圈采样每格印刷底色，得到**严格的 4 周期 蓝/绿/红/金 序列**（52 格里只有 1 格被我的取色器误判，肉眼核对是红）——这是独立佐证：任何追踪错误都会打破周期。

**拓扑真相**：
- 外圈 52 格，每象限 13 格 = 5 个直边格 + 8 个绕自家机库的转角格
- 四个转角各是**一个正方形被对角线切成两个独立半格**，各带一个圆点（在 (363,372) 做垂直分隔线的颜色扫描确认：金 `(231,186,71)` → 分隔线 `(90,113,121)` → 蓝 `(52,90,135)`）——这是前两版都漏掉的细节，也是"每象限只算 7 格"的由来
- 四条彩色虚线飞行航线**全部起止于对角半格**，跨度都是 +12 印刷格；棋盘上 52 个圆点**全是素圆点，没有任何星标/飞机图案**——我之前写在本文件里的"星标飞行格在顶边正中"是编的
- 按用户规则（待飞区在"上一家"颜色的三角格旁、该三角格是第 1 格）定位：四队待飞区 = 印刷 #4/#17/#30/#43，第 1 格 = #5/#18/#31/#44，而这四个三角格的印刷底色恰好分别是绿/红/金/蓝 = 各自"上一家"的颜色，**四重独立吻合**
- 归家小道口 = #2(红)/#15(金)/#28(蓝)/#41(绿)，各队从自己待飞区走 **50 步**到达自己的小道口（四队全部一致）

**新模型**（`Track.kt`）：`RING_SIZE=52`；`path 0` = 待飞区；`path 1..50` = 跑道（"第 N 格" = `path N`）；`path 50` = 自家小道口；`path 51..56` = 归家小道 6 格；`FINISH_PATH=57`。`ringIndex(team,path) = (standbyRingIndex(team) + path) % 52`，纯查表无特例。**飞行格收敛到一个常数**：四队各自的飞行格全部落在 `path 18`，终点全部 `path 30`（`FLIGHT_PATH=18`/`FLIGHT_ADVANCE=12`）——这不是凑的，是从像素独立推出来的四队一致结果，是拓扑推导正确性最强的旁证。因为航线是按颜色编码的，只能从**自己颜色**的飞行格起飞（红方经过绿方飞行格时 path 是 5 而不是 18，不触发）。

**两个症状分别对上了**：
- "起飞到第一格" —— 旧代码 `OnPath(0)` 渲染在印刷 #5，那正是用户定义的第 1 格；待飞区（#4）在代码里根本不存在（`positionFor` 只有机库/跑道/小道三个分支）
- "累加之前的6格" —— 旧表在每个起飞格处一步跨 **7 个印刷格**（378px ≈ 286mm，棋盘才 950mm），静默跳过 6 格。骰子只有 1-6 点，所以**起飞后第一步永远恒等于多走 6 格**，不是像 6，是数学上等于 6。这个 7 格跨步正是 2026-08-17 上午我给 `localIndex == 0` 加的那个特例分支引入的

**`BoardGeometry.kt` 重写**：`RING_PX` 52 项实测表 + 每队自己实测的归家小道表（不再靠旋转绿方数据推算，审计实测旋转推算有 ~22px 误差）。删掉了 `GREEN_LOOP_PX`（7 点表）和 `localIndex==0` 特例。座位旋转（`configureSeat`）保留，它是整块棋盘的刚体旋转，跟格子编号无关。

## 同一轮修掉的其余确认缺陷（2026-08-17）

- **交互 blocker：点自己的飞机会帮 AI 走棋。** `BoardStage` 的守卫检查的是"被点棋子是不是我的队"，从不检查"现在是不是我的回合"；而 `engine.roll()` 在骰子翻滚**之前**就把 phase 置成 `AWAITING_MOVE` 并填好 AI 的合法走法，所以 AI 那 ~736ms 翻滚窗口里你点自己飞机，走法查找（只匹配 `pieceIndex`、不看 `it.team`）会命中 **AI 的**走法并应用——AI 的棋子动了，你点的没动，而调试日志还会打印 `move[human]: team=YELLOW` 把下一次排查带偏。修法：守卫加 `engine.state.currentTeam == humanTeam` 和 `!isRolling`，查找加 `it.team == humanTeam`，并在 `GameEngine.applyMove` 里加一道 `move.team != state.currentTeam` 的兜底拒绝（有单测锁定）。
- **交互 blocker：可永久卡死。** `rollAndAdvance` 没有 try/finally，而它挂在 `LaunchedEffect(revision)` 上、任何 `publish()` 都会 `revision++` 取消该协程；取消若落在 `tumbleTo`(736ms) 或 `animateMove`(384ms) 挂起期间，`isRolling = false` 永不执行 → 之后 AI 不再摇、点骰全被拒、HUD 永远"对手思考中…"，只能重启。**修法不是加 try/finally 就完事**（那只是止血）：把 AI 回合驱动从"状态键控的 effect"换成 `LaunchedEffect(Unit)` 常驻轮询循环（20Hz）。这个驱动已经错过两次且方向相反——键 `(phase,currentTeam)` 时摇 6 后不重触发（键值绕回原值），改键 `revision` 后变成可被中途取消；键 `Unit` 同时消除这两类问题。try/finally 也一并加上了。
- **规则漏实现：僚机不可拆分**（设计文档 §6"僚机整体不可拆分单独移动其中一架…以'这一格的飞机整体'为单位选择"）。`legalMoves` 原来逐架发独立走法，`GameState.teammatesSharing` 是为这条规则准备的却只被测试用过。修法：`Move` 新增 `groupPieceIndices`，`legalMoves` 对在途棋子按所在 path 分组、每组只发一条走法，`resolve` 把整组一起搬（飞行也整组一起飞），渲染层 `animateMove` 改成接收索引列表、整组一起滑。机库里的飞机不算一格，仍逐架起飞。
- **动画所有权/取消**（`PlanePieceRenderer`）：`publish()` 会硬置所有棋子位置，落在滑动动画中途就会把棋子瞬移到终点、而仍在跑的 lerp 又把它拽回来——滑动看起来就成了"瞬移+抖动"。修法：每颗棋子一个 generation 计数器，新动画自增并抢走所有权，旧循环发现不再持有就自行退出；`render()` 跳过正在动画的棋子；`finally` 里只释放自己仍持有的棋子。
- **多队同时被吃时 `MoveOutcome` 串味**：`capturedTeam` 会被后一个队伍覆盖。新增 `capturedPieces: Map<Team, List<Int>>` 作为权威记录，旧两个字段保持单队形状不破坏调用方。
- **飞行后 `pairedWithPieceIndex` 残留**：起飞前那格组成的僚机在落点可能不存在了，现在飞行后清零再由 `resolveCellEffects` 重算。
- **骰子点击被吞**：骰子守卫读的是 Compose 快照 `phase`/`currentTeam`，而它们要到下一次重组才刷新，所以每次走完棋后约 384ms 内点击被静默丢弃——恰好是"摇到6、再摇一次"那个时刻。全部守卫改读 `engine.*` 实时状态。
- **双击"开始游戏"**：会启动两个互相打架的选座动画。加 `isSeating` 闭锁 + `engine.phase != SETUP` 判断。

**单测**：从 56 条扩到 **64 条全绿**。新增/重写的关键几条直接锁死这次的 bug：`consecutive path values are always adjacent printed cells`（"+6"的算术形式）、`adjacent ring cells are always about one printed cell apart`（几何形式，>95px 即判失败）、`path 1 is cell 1 and sits one printed cell past standby`（起飞落点）、`every team's own flight square is the same path offset`、`seat rotation keeps the board rigid`、僚机整组移动/反吃、`applyMove` 拒绝越队走法。

**测试里的一个坑值得记**：老测试用 `yellow path 26` 这类字面量表达"和红方 path 5 同格"，换了环长之后这些字面量指向无关的空格子，测试要么**空过**（断言"没发生捕获"而恰好真的没发生）要么因错误原因失败。新增 `RingCoincidence.kt` 测试辅助（`pathSharingCellWith` / `reachableRingCells`）让这类跨队重合由拓扑算出来，意图不会随环长腐坏。同理，我自己新写的一条断言（"待飞区应比第1格更靠近机库"）也**一开始就是错的**：第 1 格是紧贴机库角的三角格，待飞区在它前一格、离机库中心更远——已改成断言"两者相邻且不同"。

**验证情况**：`assembleDebug` + 64 条单测全绿，装到真机（`PB311XKGL4160087B`）启动无 crash、crash buffer 干净。**但整局玩法（起飞落点、走格、僚机、飞行格、吃子）都还没有真机实操验证**——真机截图工具在这台设备上一直报错（`ADB screencap returned non-PNG output`），我这边看不到画面，也无法驱动体积式交互。这是本次交付**唯一**缺的一环，需要你戴上头显实际走一局。

## 规则改动：取消棋盘待飞格，待飞改为机库内的视觉状态（2026-08-17，用户指定）

用户明确要求："不另设待飞区，摇到6后选择飞机库的飞机待飞时——飞机要放大、上升3厘米、高亮显示，并且开始播放模型动画。"

**规则层**：`path 0` 仍然是"待飞"这个**状态**，但不再是棋盘上的一格——飞机留在自己的机库停机位上。因此 `Track.isOnSharedLoop(0)` 现在返回 **false**（之前是 true），待飞飞机吃不到别人、也吃不到，`ringIndex(team, 0)` 直接抛异常。跑道编号相应改为以**第 1 格**为锚点：`ringIndex(team, path) = (cell1RingIndex(team) + path - 1) % 52`，`cell1RingIndex` = 红 5 / 黄 18 / 蓝 31 / 绿 44（原来锚在待飞格 4/17/30/43，现在那四格纯粹是别队路过的普通格）。**这样"在待飞摇到 N 就落在第 N 格"成立**，跟用户数格子的方式一致。`LOOP_LAST_PATH`(50)、`HOME_LANE_SIZE`(6)、`FINISH_PATH`(57)、`FLIGHT_PATH`(18)、`FLIGHT_ADVANCE`(12) 数值都没变（`FLIGHT_PATH=18` 在新锚点下依然四队一致，又一次交叉验证）；每队现在走 50 格印刷跑道、跳过自家小道口到自家第 1 格之间那两格。

**僚机分组要排除待飞**：两架待飞的飞机停在**两个不同的停机位**上，不是同一格，所以 `legalMoves` 的分组现在只对真正在跑道上的棋子生效，待飞和归家小道的棋子各自单独出走法。（如果不改，两架待飞飞机会被当成一个僚机整体一起被迫起飞。）

**视觉层**（`PlanePieceRenderer`，全部按用户列的四项）：
- **放大**：pivot 缩放 `STANDBY_SCALE = 1.6f`
- **上升 3 厘米**：`STANDBY_LIFT_M = 0.03f`，叠加在常规 `PIECE_LIFT_M` 之上
- **高亮**：pivot 下方挂一个 `MeshResource.createCylinder` 薄圆盘，`UnlitMaterial.create(BlendingMode.ADD)`（加色混合，读起来像发光而不是贴了块色板）+ 队伍色，只在待飞时 `enabled = true`。**故意不去改飞机自身材质**——16 架棋子是 `clone(shouldShareMaterialInstance = true)` 出来的，共享材质实例，染色一架会把同队四架一起染了。
- **播放模型动画**：`entity.findSkinnedMeshEntity()` + 遍历整个子树收集 `getAnimationResources()`，然后 `playAnimation(...)`；退出待飞时 `stopAllAnimations()`。**必须遍历而不能按单一形状假设**：离线拆包发现四个素材是两种不同的做法——`red_plane` 有 `SkelRoot`/`Skeleton`/`SkelAnimation`（骨骼动画），而 green/blue/yellow 没有骨骼、是 `xformOp:rotateXYZ.timeSamples` 这类**节点变换动画**（螺旋桨），两者的资源挂在不同实体上。装机实测每队找到的动画条数：**红 2 / 黄 23 / 蓝 7 / 绿 12**，四队都有，功能不会空转（这条数字有 logcat 证据，见下）。

**幂等性**：待飞视觉在 `render()` 里由游戏状态驱动，但只在**状态翻转**时才动作——`playAnimation` 会重启片段，如果每次 `publish()` 都重发就会一直抽搐。用 `standbyVisual` 集合记录当前处于待飞外观的棋子来判断翻转。

**实现时踩的两个小坑**（都是我的疏漏，编译期就暴露了）：`BlendingMode` 的加色值叫 `ADD` 不是 `ADDITIVE`；以及我给动画插值新加的内部类也叫 `Track`，把游戏层的 `Track` 遮蔽了导致 `Track.isInStandby` 报未解析——已改名 `SlideTrack`。

**验证情况**：`assembleDebug` + **65 条单测全绿**（新增/改写了"待飞不在共享跑道上""待飞在自己停机位上（四个 index 各自对应自己的 pad）""从待飞摇 3 落在第 3 格"等）。装到真机启动无 crash，且 **logcat 已确认四队动画条数**（`adb -s PB311XKGL4160087B logcat -d -s SpaceFlightChess:E | grep "model animations"`）。**但四项视觉效果本身（放大/升高/高亮/动画在跑）没有画面验证**——真机截图工具在这台设备上一直报错，我看不到画面；需要你摇到 6 选一架飞机，亲眼确认这四项都对。

## 棋盘再推远 + 骰子改成"从玩家抛到棋盘中央"（2026-08-17，真机反馈）

用户反馈："打开应用玩家在棋盘中间，稍微设远一点距离。另外投骰子时（放大、高亮）从角色处投到棋盘中间。"

**棋盘距离**：`BOARD_FORWARD_OFFSET_M` 0.55 → **0.85**。0.55 时近边只在身前约 7cm、棋盘footprint 仍然包住双脚，所以读起来还是"站在棋盘里"；0.85 时近边约 0.37m（脚前有空隙）、远边约 1.32m（射线仍可及）。仍是判断值不是量出来的。

**骰子改成真的"抛"**（`DieRenderer`）：
- 静置点从原来固定的 `(0.50, 0.025, 0.50)` 改为**玩家身侧**，由 `BoardStage` 用放置棋盘的**同一个朝向向量**算出来（`setRestPosition`），所以两者永远在同一条轴上——`DIE_REST_BACK_M`(0.73，即棋盘偏移量减 0.12) 往玩家方向、`DIE_REST_SIDE_M`(0.22) 侧移避免正对视线中央、`DIE_REST_HEIGHT_M`(0.14) 约手部高度。为此 `BoardStage` 把实际用到的朝向记成 `resolvedForward`（兜底朝向或 HMD 朝向），否则骰子和棋盘会在"HMD 数据到没到"这件事上各算一套。
- `tumbleTo` 改名 `throwTo`：放大(`THROW_SCALE=1.7`) + 高亮 → 沿**抛物线**（`THROW_ARC_HEIGHT_M=0.16`，翻滚与位移同步推进）飞到棋盘中心 → 定格在该点数朝上 → 停留 900ms 让人看清 → 弧线飞回静置点并恢复原样。
- **高亮用加色发光球壳**（`MeshResource.createSphere` + `UnlitMaterial.create(BlendingMode.ADD)`），不是待飞飞机那种地面光圈——骰子在半空，地面光圈没有意义。
- **回程这一段是我加的，用户没要求**：不加的话骰子会停在棋盘正中心，盖住四队共用的终点风车图案，而且点击目标每摇一次就换位置。加回程让这两件事都稳定。

**验证情况（这轮改用模拟器，用户明确要求先用模拟器）**：注意排查时发现真机当时前台是 `tech.illusion.spacestack`（另一个项目），我反复抢启动本来就不合适，这也是真机 `app launch` 连续失败的原因之一。
- 模拟器截图**首次拍到了完整可用画面**：开局面板尺寸正常（四色阵营 + 开始游戏按钮，不再填满画面——历史记录里那个"面板一挂就填满/不出画"的问题在这一版没有复现）、棋盘在更远的位置、HUD 正常。
- **抛掷轨迹有实数证据**（临时日志，已移除）：`START from=(0.22,0.14,0.73) glow=true` → `LANDED at=(0.0,0.025,0.0)`（正是棋盘中心）→ `RETURNED` 回到原位；且落定朝向与点数一致（摇 6 → `pitch=-90`、摇 4 → `roll=-90`，对得上实测点数表）。
- **新待飞规则也被日志端到端证实**：`move[ai]: BLUE from=InHangar to=OnPath(0) absCell=null`（起飞进待飞，`absCell=null` 证明待飞不在共享跑道上）→ 紧接着 `roll: BLUE value=1` → `to=OnPath(1) absCell=31`，而 31 正是蓝方实测的第 1 格。"待飞摇 N 落第 N 格"成立。
- `assembleDebug` + 65 条单测全绿，模拟器启动无 crash。
- **仍没有视觉验证的**：骰子静置在玩家身侧、位于模拟器固定相机的视野下方，拍不到；所以"放大/发光/抛物线"这三项的**观感**（幅度、速度、是否好看）没有画面确认，只有坐标和状态标志的证据。待飞飞机那四项视觉（放大/升高 3cm/光圈/动画）同理仍未有画面确认。

## 两架待飞飞机一起起飞（真实 bug，我上一轮的修改没落地）+ 同色格跳跃规则（2026-08-17）

**Bug：两架待飞飞机会一起飞走。** 用户反馈"连续两次投到6都选择飞机待飞，下一次投任意数字选择飞机飞行会有两架同时一起飞过去（AI 出现过）"。

这个 bug **在当时的版本里确实存在**，而且原因很难看：我在"取消待飞格"那一轮**声称**已经把待飞排除出僚机分组，但那次 `replace` 的锚点文字对不上（我写 `design doc section 6`，文件里是 `design doc §6`），**静默失败没落地**，文件里一直还是按原始 path 值 `groupBy` 的老代码。两架待飞飞机都在 `path 0`，于是被当成一个僚机整体，一起走。

**教训**：那一轮我对部分 `replace` 加了 `assert`、对这一处没加。以后用脚本改代码，**每一处替换都要断言锚点存在**，否则会出现"我以为改了、测试也没覆盖、于是一路带到真机"的情况。这次先写了一条会失败的测试（`two planes in 待飞 do not fly together`）确认 bug 真实存在，再修，修完该测试转绿。

**新规则：同色格跳跃**（用户指定："当我走到对应本方颜色的棋格时，应该直接跳到下一个本方颜色的棋格（如果是遇到飞行路线则可以直接飞过去，例外情况是存在叠机在行经的格子上时则不能采用该操作）"）

这条规则能干净落地，靠的正是之前实测出来的**印刷底色 4 周期**（蓝/绿/红/金）：下一个同色格永远是 +4 格。而且因为每队第 1 格锚在自己机库，算下来**四队的自家颜色格恰好都是 `path % 4 == 2`**（每圈 13 个）——又一次四队一致的收敛，和当初 `FLIGHT_PATH=18` 四队一致同样性质，是环编号正确性的独立佐证。`FLIGHT_PATH=18` 本身就满足 `18%4==2`，与"贴图把每条航线画在其主人自家颜色格上"完全吻合。

实现（`Track.isOwnColourCell` / `Track.colourHopTarget` / `GameEngine.resolve`）：
- 落在自家颜色格 → **只触发一次**：若该格是自家航线起点就飞 `FLIGHT_ADVANCE`(12)，否则跳 `COLOUR_CYCLE`(4) 到下一个同色格。**必须只触发一次**——周期是 4，跳完的落点必然又是自家颜色格，不设限会无限递归（飞行落点同理）。
- **叠机拦截**：跳/飞会跨过的格子上若有任何一队的僚机（同格 2 架以上），本次跳/飞取消，飞机停在骰子本来该走到的格子，并在 `MoveOutcome.carryBlockedByStack` 标记。只检查**跨过**的格子、不含落点——落在僚机上是另一条早已定好的规则（对方僚机免疫单机、双方共存），把落点也当拦截会改掉那条规则而不是实现这条。
- **两个必须设的护栏**（不设就会炸）：① 归家小道每一格都是该队自家颜色，不限制的话在小道里每走一步都会触发跳跃；② 小道口（`path 50`）也是自家颜色（正因如此它才是该队的口），从它起跳会一头扎进小道深处 4 格。所以 `isOwnColourCell` 只在共享跑道上成立，且 `colourHopTarget` 在落点离开跑道时返回 null。

**验证情况**：单测 65 → **77 条全绿**，新增 12 条覆盖：四队自家颜色格一致性、跳 +4、只跳一次不连锁、航线起点飞 12 而不是跳 4、叠机拦截跳、叠机拦截飞、单机不拦截、小道口不跳、小道内不跳、跳到落点仍能吃子、僚机整组一起跳。

另外**在模拟器上用"四队全 AI 自走棋"的临时 build 拿到了真实对局证据**（临时代码已按断言移除）：日志里 YELLOW 走到 path 6 触发跳，而它**下一次移动的 `from=OnPath(10)`**——正好是 6+4，跳跃落点由后续日志交叉验证；RED 走到 10 跳到 14，再走到 16、18 时触发飞行。四队多次命中，落点全部 `≡2 (mod 4)`。叠机拦截在自然对局里没触发（需要僚机正好压在跨越格上），由单测覆盖。

**没验证的**：跳跃/飞行目前沿用普通移动的直线滑动动画（`animateMove`），**没有专门的"跳格/沿航线飞"视觉**——设计文档 Week 4 的弧线飞行动画仍未做。另外这条规则改变了棋局节奏（每圈 13 个自家颜色格，跳跃会明显加快推进），实际手感需要你玩一局判断是否过快。

## 按用户提供的《飞行棋规则检查指南》做全量非法路径核查（2026-08-18）

用户提供了一份规则检查文档（`~/Downloads/deepseek_markdown_20260817_3c4238.md`，15 节），要求逐条核查是否存在非法路径。做法：按文档自己的章节切成 7 个维度并行审计，每条 blocker/major 发现再交给独立 agent **专门试图推翻**。结果：**7 条确认、5 条被推翻、24 条次要/未定义、15 条明确核对无问题**。

### 修掉的 4 条（都已补测试，单测 77 → 85 全绿）

1. **摇到 6 且无任何合法走法时，给了额外一次摇骰而不是换手**（文档 §1.2 最后一条）。这不是"规则未定义"，而是**违反本项目自己的设计文档**——设计文档第 54 行原文：「合法集合为空 → 本次点数作废，直接换下一家（连续摇 6 的额外回合同样适用此判定）」。而且内部不一致：同一个 `moves.isEmpty()` 分支，点数 1-5 会换手、点数 6 却不换。原因是空集分支复用了 `resolveEndOfRollSegment`（它对 6 一律给额外回合）。修法：抽出 `passTurn`，空集分支无论点数一律换手；`resolveEndOfRollSegment` 只留给"走完棋之后"的路径。
2. **僚机一旦拐进归家小道就会被拆散**（文档 §6.2「叠棋可以进入终点航道，但必须保持整体」）。三个维度独立提出、验证确认。根因：`legalMoves` 的分组键是 `isOnSharedLoop`，小道格不参与分组，于是整体进小道后其中一架可以单独走。修法：分组条件改为"在跑道上 **或** 在归家小道上"，待飞仍然排除（两架待飞在两个不同停机位，不是同一格）。
3. **`GameEngine.roll()` 对骰子值零校验**（文档 §1.1「拒绝 0、7、负数」）。出厂路径不可达（`DiceEngine` 是 1..6 且生产代码只用默认实现），但 `DiceEngine` 是 `open` 且构造注入的、测试里已经在用 `ScriptedDice` 走这道门；0 或负数会一路流进 `legalMoves`/`resolve`，产生"零格移动"（还能被同色跳跃再带走 4 格）和倒退移动。修法：`require(value in 1..DiceEngine.FACES)`——坏骰子是契约违反而非合法输入，宁可大声失败也不要静默污染棋盘。
4. **`BoardGeometry.homePosition` 完全忽略了自己的 `team` 参数**（文档 §2.2/§12.3 状态与坐标一致性）。四队到达终点的飞机全都堆在同一个点上，不同队伍同 index 的飞机坐标**逐字节相同**。修法：终点位置沿各队自己的归家小道方向偏移一点（`FINISH_ARM_BIAS`）。
5. 附带修了 **`GameState.teammatesSharing` 用裸 `PieceState` 相等判断**，会把待飞（都在 path 0 但分处四个停机位）、小道格、已完成（都在 `FINISH_PATH`）都报成"同格僚机"。它目前只被测试用到，但**这正是当初"两架待飞一起飞"那个 bug 的同一种误编码**，所以顺手改成基于真实格位判断。

### 被推翻的 5 条（记录下来，避免以后再当成 bug）

- **"一次摇骰结算两次吃子，飞机在自己已离开的格子上吃了子"** —— 我在上一条消息里亲口怀疑过这是违反 §7.1「不能在路途中吃掉」的 blocker，**这个怀疑是错的**。文档 §4.3 规定的结算顺序本身就是「3 落点吃子 → 4 触发跳跃/飞行」，所以"在触发格合法停留并吃子、然后跳走"正是文档要求的行为；§7.1 说的"路途中"指**跨过**的格子，不是合法停留过的落点。当前实现（落点 `resolveCellEffects` → carry → 再 `resolveCellEffects`）与 §4.3 一致。
- **"叠棋阻挡对普通移动无效 = 规则不一致"** —— 事实成立（`stackOnTraversedCell` 只被跳跃/飞行调用，普通骰子移动只算终点、不走格），但**不是不一致，是一个用户没回答过的规则版本问题**：用户当初的指令是"存在叠机在行经的格子上时则不能采用**该操作**"，该操作指的是同色跳跃/飞行；"叠棋是否阻挡普通移动"文档 §0 明确列为版本分歧点。已列入下面待你拍板的清单。
- 另外三条（`forceState` 能把已结束对局拉回、`MoveOutcome` 多队吃子归属、以及叠棋阻挡的另两种表述）被降级为注释/调试口子级别。

### 文档 §0 要求先确认的规则版本 —— 本项目目前的实际状态

这几条**代码里现在没有答案**（不是 bug，是未定义），文档明确说必须先定版本否则后面必然出错：

| 规则 | 本项目现状 |
| --- | --- |
| 连续三次 6 怎么处理 | **完全没有定义**，也没有任何计数器；不封顶连摇可达（不构成卡死，AI 每轮 6 的开销约 3.0s 全在 `delay()` 里，不会饿死 UI） |
| 叠棋是否阻挡敌方普通移动 | **不阻挡**（只阻挡同色跳跃/飞行） |
| 安全格有哪些 | **完全没有安全格概念**；归家小道靠"私有"而非"安全"实现，僚机免疫只针对单机 |
| 吃子后是否奖励再摇一次 | **没有奖励**（纯属省略，未明示） |
| 吃叠棋是整叠还是一架 | 整叠回机库；但**从不比较叠棋大小**——2 叠可以反吃 3 叠或 4 叠 |

另有一条"意外的安全格"值得记：审计发现每队各有 8 个格子对**某一个特定队伍**永久免疫（因为该队的 path 编号永远不会映射到那些环格），这是编号几何的副产物、不是设计出来的，文档 §8.1「安全格列表是否完整」这条因此实际上是"存在未记录的隐式安全格"。

## 规则版本拍板并实现（2026-08-18，用户对文档 §0 逐条给出答案）

用户针对《飞行棋规则检查指南》§0 要求先确认的分歧点给出了答案，五条全部实现，单测 85 → **100 条全绿**。

| 规则 | 用户裁定 | 实现 |
| --- | --- | --- |
| 连续三次 6 | 阵营内**选择**一架飞机飞回仓库 | `Track.CONSECUTIVE_SIX_LIMIT = 3`；第三次 6 时引擎把"送回机库"做成普通 `Move`（`to = InHangar`）放进 `legalMovesCache`，**复用现有"点飞机选择"输入链路**，不新增 phase、不改表现层。惩罚落子后一律换手（不给额外回合）。计数器在换手/开局/重开/`forceState` 时清零。 |
| 叠棋阻挡普通移动 | 阻挡；**超出点数从叠棋处开始倒飞** | `GameEngine.destinationAfterBlocking`：前进到第一个叠棋所在格，剩余点数改为**向后**走。落在叠棋上不算阻挡（那是正常抵达，走吃子/免疫规则）。 |
| 安全格 | **仅归家小道**；且不会有其它颜色飞机在那里 | `Track.isSafeCell(path)` = 归家小道 + 终点，并在 `resolveCellEffects` 里**显式**拦一道。 |
| 吃子奖励再摇 | 没有 | 本来就没有，补了一条测试锁死。 |
| 吃叠棋 | **少的不能吃多的** | `resolveCellEffects` 由 `otherPaired && !moverPaired` 改为 `otherAtDest.size > ownAtDest.size` 才免疫。这条**包含**了旧的"单机吃不了僚机"（2>1），并额外堵住了"2 叠反吃 4 叠"。 |

**实现时的几个判断（都不是用户指定的，需要时可推翻）**：
- **倒飞只认第一个叠棋**：倒飞途中若又跨过第二个叠棋，不再重新判定。
- **倒飞下限是第 1 格**：飞机不能倒退回待飞（那是机库停机位、不是棋盘格）或退出棋盘。
- **只有跑道上的叠棋会阻挡**：归家小道格没有共享环编号，`ringIndex` 在那里不成立，所以小道内不判定阻挡。
- **阻挡对所有队伍的叠棋一视同仁**（包括自家叠棋），与已有的跳跃/飞行阻挡保持一致；文档 §6.3 原话只问"敌方是否可以通过"，所以"自家叠棋是否也挡自己"严格说仍未定义。
- **惩罚时 AI 交出推进最少的那架**（`PlaneAi` 新增分支）；否则所有惩罚走法在比较器里全部打平，会随机挑一架。
- **僚机整组受罚**：`penaltyMoves` 把同格的僚机作为一个整体送回机库，因为只送一架会拆散僚机。

**又一次踩到同一个坑**：`penaltyMoves` 最初按 path 分组，于是两架待飞飞机（都在 path 0、但分处两个停机位）被当成一个僚机整体一起送回机库——**这已经是同一种误编码第三次出现**（前两次分别是 `legalMoves` 和 `GameState.teammatesSharing`）。测试当场抓到。教训：凡是"按 path 分组"的地方，必须显式排除待飞；path 0 不是一个格子。

**验证**：100 条单测全绿（新增 15 条覆盖三连 6 惩罚/计数器重置/僚机整组受罚/待飞不合并、倒飞的四种情形含只认第一个叠棋与下限、少不能吃多与等量可吃、安全格集合与小道内不吃子、吃子不给额外回合）。模拟器上跑了一局四队全 AI 自走棋：51 次摇骰、28 步落子、6 次同色跳、1 次吃子、四队轮转均匀（12/12/14/13）、棋子推进到 path 41、无 crash。三连 6（1/216）和撞叠棋在这局里没自然出现，那两条只有单测覆盖。

## 骰子改为"停在当轮队伍面前的棋盘空位"（2026-08-18，用户标注图指定）

用户给了一张标注图，在黄方机库斜内侧那块空白星空上画圈标了"骰子"，要求：「每回合骰子默认放到本轮玩家或 Ai 的面前棋盘上（靠近阵营的左边空位置处）」。这取代了上一轮"骰子静置在玩家身侧手部高度"的做法——现在骰子在**棋盘上**，并且**跟着回合走**。

**位置怎么定的**（没有靠比例估算）：标注图是一张裁剪图，我用两个共同特征（红色虚线航线箭头、黄方机库左上角）把它对齐回 1254×1254 原图，得出黄方口袋约在 **(769, 1008)**。换算成绿方参照点是 **(485, 246)**，写进 `BoardGeometry.GREEN_DIE_SLOT_PX`，其余三队沿用机库那套 90° 旋转推导。**四个旋转位置全部验证过**：各自离最近印刷格至少 114px，取样亮度 14-55（暗色星空），所以骰子不会压住任何印花。

**实现**：
- `BoardGeometry.dieSlotPosition(team)` —— 跟随座位旋转，所以人类自己的槽位会落在离他最近的那侧。
- `DieRenderer.glideToRest(position)` —— 换手时**滑行**过去而不是闪现（20 帧 + 小弧线）；投掷进行中是 no-op，但会记下新槽位，投掷结束落回时自动生效。
- `BoardStage.syncDieSlot()` —— 在每次状态变化后调用（不是监听某个事件），所以不管回合是怎么变的都能自我纠正：普通换手、摇 6 的额外回合、三连 6 惩罚、重开都一样。选完阵营后棋盘刚转过，也会重新摆一次。
- 静置高度从手部高度 0.14m 改为贴板 0.018m（`DIE_REST_HEIGHT_M`），因为它现在在棋盘面上。
- 投掷仍是"从当轮队伍的槽位 → 棋盘中心 → 飞回该槽位"，所以"从角色处投到棋盘中间"这条依然成立——人类的槽位就在他自己那一角。

**验证**：100 条单测全绿；模拟器上跑四队全 AI 自走棋，日志确认骰子**四个队伍的槽位全部访问到**，四个坐标恰好是同一点的 90° 旋转（离盘心 0.308m，稳稳在 0.475m 半宽内），高度 0.018m，轮转顺序与回合顺序一致。临时验证代码已按断言全部移除，模拟器与真机装机启动均无 crash。

**没验证的**：骰子在槽位上的**观感**（0.308m 这个半径是否太靠内/太靠外、滑行速度是否合适）没有画面证据——模拟器固定相机拍不到那一小块区域。需要你戴上看一眼。

## 下一步

1. **最优先：真机实操走一局。** 52 格重构 + 待飞规则改动了整个坐标/规则地基，但只有单测和"启动不崩"两层证据，**没有任何画面证据**。真机截图在这台设备上用不了（`pico-cli capture screenshot` 报 `ADB screencap returned non-PNG output`），交互也没法脚本驱动，所以需要你自己确认这几项：
   - 摇到 6 选机库飞机 → 飞机是否**留在机库停机位上**，并且同时出现：放大、上升约 3cm、脚下一圈队伍色发光、模型自身动画开始播（螺旋桨转/骨骼动）
   - 在待飞状态再摇一次（比如 3）→ 是否落在**第 3 格**（而不是第 4 格或第 9 格）
   - 两架飞机同时处于待飞 → 点其中一架应该**只有那一架**起飞（两架不同停机位，不算僚机）
   - 棋盘距离（0.85m）是否舒服；**骰子停在当轮队伍槽位上的位置感**（离盘心 0.308m）、换手时的滑行速度、抛出去的弧线/放大/发光幅度是否合适（这几个数都是判断值，可调）
   - **同色格跳跃的手感**：每圈有 13 个自家颜色格，跳跃会明显加快推进，是否过快
   - **两架飞机同时待飞时点其中一架，是否只有那一架起飞**（这是刚修的 bug，值得专门试一次）
   - 沿跑道连续走几步 → 每一步是否**只跨一个印刷圆点**，尤其经过四个机库转角时不应该突然跳一大截
   - 走到 `path 18`（自家颜色那个飞行格，虚线箭头起点）→ 是否沿虚线飞到 12 格外
   - 绕完一圈 → 是否从自家颜色的小道口拐进归家小道，6 格后精确点数到终点
   - 两架自家飞机同格 → 点其中一架时**两架应该一起走**（僚机不可拆分）
   - AI 摇骰翻滚期间点自己的飞机 → 应该**没反应**（而不是帮 AI 走棋）
   - 连续玩若干回合 → 不应再出现"AI 不再摇骰 / HUD 永远显示对手思考中"的卡死
2. 若发现落点仍有系统性偏移，**先查日志再改代码**：`adb -s PB311XKGL4160087B logcat -c` 清缓冲、复现、然后 `adb -s PB311XKGL4160087B logcat -d -s SpaceFlightChess:E`，看 `roll:` 和 `move[...]:` 两类行的 `from`/`to`/`absCell`。`absCell` 现在是 0..51 的印刷格编号，可以直接对着 `BoardGeometry.RING_PX` 查坐标。注意 logcat 是 2MB 环形缓冲、系统日志很密，超过十几分钟的记录会被冲掉，要复现完立刻捞。
3. **面板一挂 3D 内容就不出画**这个模拟器问题仍未解决（见上文 2026-08-14 记录），且模拟器相机取景也不可靠。真机上如果面板尺寸/位置不对（历史记录里 `StartPanel` 曾经填满整个画面），那是这条未修问题的延续，不是这次重构引入的。
4. 棋盘摆位（`BOARD_FORWARD_OFFSET_M = 0.55f`、`BOARD_HEIGHT_BELOW_HEAD_M = 0.60f`）是估的不是量的，等你戴上头显给个手感反馈再调。
5. 还没做：骰子的抓取甩动手势（设计文档 Week 4，目前是点击）；飞行格专属的"起飞→爬升→转弯→跨棋盘弧线→降落"动画（目前飞行只是同一条直线滑动到 12 格外）；僚机叠放的视觉细节（目前两架重叠只靠 `JITTER` 微偏移区分）；被吃回机库仍是瞬移无动画。
6. 未修的次要项（审计确认但这轮没动）：摇到 6 且无任何合法走法时仍然给额外一次摇骰机会（设计文档的"无合法走法"条款读作应该换手）；`PlaneAi` 的最终平局判据按飞行**前**的落点打分，所以可能主动放弃一次 +12 飞行收益。两条都不影响正确性，只影响手感/强度。
7. 确认 5 个 `.usdz`（4 飞机 + 1 骰子）同场景 17 个实体的性能（设计文档第五章风险项）——骰子法线贴图 1.6MB 偏重，真机若掉帧优先压缩贴图。

## 构建、安装、运行

```bash
export PICO_HOME='/Users/zohar/Library/PICO/sdk'
export JAVA_HOME='/Applications/Android Studio.app/Contents/jbr/Contents/Home'
./gradlew testDebugUnitTest    # 56 个纯逻辑单测
./gradlew assembleDebug
bash ../.claude/skills/spatial-design-first-build/scripts/device-lock.sh acquire "<owner>" 120 emulator-5554
pico-cli app install app/build/outputs/apk/debug/app-debug.apk --device emulator-5554
pico-cli app launch tech.illusion.spaceflightchess --device emulator-5554
sleep 6 && adb -s emulator-5554 shell dumpsys activity activities | grep mFocusedApp   # 确认真的是本应用，不是别的会话的
pico-cli capture screenshot --device emulator-5554 -o .spatialsdk/ui-verify/<round>.png
pico-cli app stop tech.illusion.spaceflightchess --device emulator-5554
bash ../.claude/skills/spatial-design-first-build/scripts/device-lock.sh release "<owner>" emulator-5554   # owner 字符串必须跟 acquire 时完全一致，不能用 $$ 之类每次调用会变的值
```

## 2026-08-18 — 单机落位与"叠棋吃不掉单机"（同一个 bug）

**症状（用户报告两条）**
1. 「叠棋2个无法飞跃单机以及吃单机」
2. 「单架飞机无法摆放在棋格圆中心上」

**根因：只有一个，在渲染层。** `PlanePieceRenderer.targetPosition` 无条件套用
`JITTER[index % 4]`，与该格是否被共用无关。实测（`ringPosition` 相邻格距均值 5.93cm）：

| | 距离 | 占格距比例 |
|---|---|---|
| 单机偏离圆心 | 1.70cm | 29% |
| 叠棋两架间距 | 2.40cm | 40% |

40% 的间距让 2 个僚机看起来像**分踞相邻两格**，于是叠棋压上单机时"看起来没吃到"。
规则引擎从来是对的 —— `GameEngineTest` 现有两条回归测试直接证明 2 叠棋既能飞越也能吃掉单机
（`capturedTeam=YELLOW`）。`stackAtRing` 也确认是按**同队** `>= 2` 计数，两个异色单机不会被误判成叠棋。

**修法**
- 新增 `BoardGeometry.occupancyKey(team, pieceIndex, piece)`：两枚棋子当且仅当画在同一处时 key 相同。
  跨队是关键 —— 各队从自己的待飞区起算路径，RED path 8 与 YELLOW path 21 可能是同一格，必须同 key。
  机库/待飞是每子独占的停机位，key 唯一，永不散开。
- 新增 `BoardGeometry.coOccupantOffset(team, pieceIndex, state)`：**独占该格时返回 (0,0,0)**（即用户要求的"落圆心"）。
  真正共用时以**高度**为主分层（`CO_OCCUPANT_STEP_M = 0.014f`，叠棋就该看起来是叠的），
  横向只给 `CO_OCCUPANT_RADIUS_M = 0.008f`，够从上方数清架数又远小于格距。

**为什么把算术挪进 `BoardGeometry`**：原逻辑在渲染层用 `Vector3`，只能靠截图验证 —— 而真机
`pico-cli capture screenshot` 坏着、模拟器相机又固定，这正是几个视觉 bug 混过去的原因。
挪成纯函数后可直接单测**实际出货的代码**，不是它的副本。

**测试**：100 → 109 全绿。新增覆盖 lone-plane-零偏移（含机库/待飞/归家小道）、满机库不散开、
僚机按高度分层且横向 < 35% 格距、异色同 index 同格仍分离（旧 jitter 会让两者完全重合，
恰好发生在吃子结算前的那一瞬）。

**待你确认（无图证据）**：单机是否已正落圆心、2 叠棋是否明显读作"同一格"。

## 2026-08-18 — 走棋改为逐格 1 秒 + 移动全程播放模型动画

**需求**：走棋时播放模型动画；在棋盘上按**每隔 1 秒**的动画时长逐格移动；停到棋格上时停止模型动画。

**改前**：`animateMove` 是一条从起点到终点的直线 lerp，24 帧 × 16ms ≈ 384ms，中间格子完全不经过 ——
摇 4 点看起来是"滑过去"，不是"走 4 格"。移动期间也没有任何模型动画（动画只在待飞时播）。

**改动**
- `Move.kt` 新增纯函数 `movementWaypoints(move, after): List<PieceState>`，区分**走**和**跃**：
  - 骰子点数逐格走（`to < from` 时倒着走，即倒飞）。
  - 撞色跳 (+4) 和飞行道 (+12) 是**一次跃迁**，只加一个航点 —— 棋盘上它们本来就画成跳，不是格子。
  - 进出机库算一跳（机库/待飞是停机位，不是格）。
- `PlanePieceRenderer.animateMove(team, indices, state, waypoints)`：逐航点播放，每航点
  `CELL_STEP_FRAMES(62) × MOVE_ANIMATION_FRAME_MS(16ms) ≈ 992ms`。最后一个航点用棋子真正的落位
  （含共格偏移与待飞抬升），之前的航点用格心 + 该棋子的最终横向偏移，好让僚机在途中也分得清。
- **动画启停收归一个幂等口** `applyAnimationState(key, piece, wanted)`，
  `wanted = 待飞 || 在途(flying)`。这一步是必须的：`publish()` 会在移动途中调 `render()`，
  而原先 `applyStandbyVisual` 持有 stop —— 飞机离开待飞的那一刻正是它开始移动的那一刻，
  两者会互相掐掉。
- `SlideTrack` 改为持 `settled` + 可变的 `from`/`to`，每一跳复用。

**唯一的知情取舍**：倒飞只渲染**倒着走的那一段**。规则上飞机是先前进到叠棋格再往回退，
但 `Move` 里没有那个折返点（`destinationAfterBlocking` 算完就丢了），从 `Move` 无法重建。
要补前进段就得给 `Move` 加一个 `bouncePivotPath` 字段并在 `legalMoves` 里填 —— 本次没做，
因为会动到 `Move` 的相等性判断（`legalMovesCache` 成员检查依赖它），不属于本次需求。

**节奏可观测**：`animateMove` 结尾常驻一行日志
`walk: team=RED indices=[0] hops=4 elapsedMs=3970 perHopMs=992 path=[4, 5, 6, 7]`，
所以"是不是真 1 秒/格"可以直接从 logcat 读，不用靠截图。调节奏只需改 `CELL_STEP_MS`。

**连带影响**：所有调用方都 await `animateMove`，所以摇 6 点的一步现在约 6 秒，
`isRolling` 门禁、AI 轮询、骰子交接都随之拉长（互斥仍然正确，不会重叠）。
HUD 在走棋这几秒内显示的仍是上一回合信息 —— `publish()` 在移动结束后才发生。

**测试**：109 → 117 全绿。新增 `MovementWaypointsTest`（8 条）覆盖逐格顺序、
**四队全路径每一步都是相邻棋格**（能抓住"跳格"这一类 bug，正是当年 28 vs 52 格的同类错误）、
机库/待飞进出各一跳、撞色跳与飞行道各只加一个航点、倒飞倒着走、三连 6 惩罚一跳回库、僚机整组共用一条走法。

**待你确认（无图证据）**：1 秒/格的手感、移动全程螺旋桨是否在转、落格瞬间是否停转。

## 2026-08-18 — 回合串行化：等本轮行棋动画结束才轮到下家

**用户报告**：玩家 A 还在行棋中，玩家 B 就投了骰子；B 的飞机停的位置与 A 相同，
结果 B 先到达、A 后到达，A 被吃回机库后模型动画还在播。

**根因**：`isRolling` 这道门禁只覆盖了 AI 那条路径。人类落子这条是：

```kotlin
val outcome = engine.applyMove(move)   // 同步就把 currentTeam 推给了下家
coroutineScope.launch { animateMove(...) }   // 1–6 秒，完全不设防
```

`GameEngine.applyMove` **调用瞬间**就推进了回合，而动画滞后最多 6 秒（上一轮改成 1 秒/格之后放大成了肉眼可见的窗口）。
期间 `isRolling == false`，AI 轮询 50ms 一次，立刻替 B 摇骰。于是引擎按"A 已落地"结算了 A，
再按"B 落在同格"结算 B 吃掉 A —— 但画面上 A 还在飞。A 的动画停止判断用的是
`animateMove` 拿到的**旧快照**，快照里 A 还在棋盘上，所以它按"在棋盘上"处理，
而真实状态已是 `InHangar`。这是同一个时序洞的两个症状，不是两个 bug。

**修法（一道门禁覆盖整个回合）**
- `isRolling` 改名 `isTurnBusy`，语义从"骰子在转"扩为"本回合未结束"，
  持有区间：摇骰/落子提交 → 骰子投掷 → 逐格行棋 → `publish()` 完成。
- 人类落子路径同样持有它。**门禁在 `applyMove` 之前、`launch` 之前同步置位** ——
  若放进协程里置位，同一帧的两次点击会双双通过前面的守卫。
- 释放一律在 `finally`（门禁卡死 = 游戏直接变砖，每次摇骰和点击都短路在这个标志上，是踩过的坑）。
- 有了这道门禁，行棋期间不可能再有状态变更，`animateMove` 的快照对该回合即为终态；
  走完立刻 `publish()` → `render(live)` 再对齐一次。

**运行时警戒线**：`animateMove` 开头检查 `flying` 里有没有别队的棋子，有就打
`TURN OVERLAP: <team> started moving while [...] were still in flight`。
这不是修复，是**兜底探针** —— 这类时序洞的症状（两机汇聚一格、后到者被从自己动画底下吃掉）
几乎不可能从 bug 报告里读出来，必须让它在 logcat 里自己喊出来。

**测试**：117 全绿（无新增单测：这是 Compose 协程时序，不在纯逻辑层，
不写"测副本"式的假测试）。

**必须由你确认（我无法自动验证）**：新代码路径只有**人类点击落子**才会走到，
而模拟器/真机的选阵营面板是空间面板，`adb input tap` 打不进去，我没法自己开局。
请玩一回合后核这两条：
1. `adb -s PB311XKGL4160087B logcat -d -s SpaceFlightChess:E | grep -E "TURN OVERLAP"` —— **应当为空**。
2. `... | grep -E "roll:|walk:"` —— 每条 `roll:` 的时间戳都应晚于上一条 `walk:`，不应交错。

## 2026-08-18 — 三个音效（飞行 / 起飞等待 / 骰子）

**素材** → `app/src/main/assets/audio/`（改为 ASCII 名）：
`飞机声.mp3` → `plane_flight.mp3`，`起飞等待.mp3` → `plane_standby.mp3`，`骰子.mp3` → `dice_throw.mp3`。
`build.gradle.kts` 加 `androidResources { noCompress.add(".mp3") }`，已核实三者在 APK 内为 `Stored`（未压缩）。

**API（来自 `pico-spatial-knowledge` + SDK api-reference / documentation，非猜测）**
- `ObjectAudioComponent`（`com.pico.spatial.core.ecs`）= 移动音源，随实体变换定位；`LoadType` 同包。
- `AudioResource`（`…ecs.resource`）、`AudioPlayerController`（`…ecs.audio`，`Closeable`，
  有 `setLoop/play/pause/stop/seekTo/isPlaying/valid`）。
- `entity.prepareAudio(res)` 取控制器而不播；`entity.playAudio(res)` 立即播。
- `.mp3` 明确在支持列表内。

**⚠️ 路径必须带 `asset://`**：SDK 两份文档自相矛盾 —— 资源管理页说"相对 /assets 目录"，
ObjectAudioComponent 页在同样的 `LoadType.FROM_ASSETS` 下传的是 `asset://…`。
按前者写，运行时报
`ResourceLoadingException {INVALID_ARGS, Illegal file type, url: audio/plane_flight.mp3}`。
带 scheme 才是真实契约，也与本项目加载模型的 `asset://models/…` 一致。已写进 `loadSound` 的 KDoc。

**实现**
- **每种音效各挂一个裸子实体**（`PlanePieceRenderer.emitterFor` / `DieRenderer`），各自带
  `ObjectAudioComponent` 与独立控制器。不把两个控制器挂在同一 pivot 上：文档只说"实体需带音频组件"，
  **没说一个实体能否同时持有两个播放器**，而待飞音还没放完时走棋就可能开始。
  子实体在两种语义下都正确，且继承 pivot 变换所以仍然定位在飞机上（highlight 圆盘已是同一手法）。
- 飞行音：`applyFlightSound(key, piece, wanted = key in flying)`，**循环播放**（一步一秒，clip 比长距离移动短），
  幂等（`render` 每次 publish 都跑，重复 `play` 会打成机枪声）。与 `applyAnimationState` 在同三处驱动。
- 待飞音：一次性，挂在 `applyStandbyVisual` 已有的 false→true 转换上。
- 骰子音：`DieRenderer.throwTo` 开头 `seekTo(0)` + `play()`，挂在骰子自己的子实体上，随投掷弧线走。
- **清理**：控制器与资源不属于实体树，销毁 pivot **不会**释放它们（SDK 要求手动 `close()`）。
  两个 `detach()` 都已 stop + close 控制器与 `AudioResource`。

**失败可见性**：`AudioResource.load` 抛异常、`prepareAudio` 返回**无效**控制器（不抛），
两者都会打日志 —— 否则"资源缺失"只会表现为"游戏没声音"。这条日志正是抓出上面 `asset://` 问题的东西。

**验证**：模拟器与真机均已装好启动，`audio load failed` / `audio prepare invalid` 皆为空，
`model animations` 16 行确认 16 架飞机都走过 `attachSounds` → 32 个飞机播放器 + 1 个骰子播放器全部 `valid`。
crash buffer 干净。117 条单测仍全绿。

**待你确认（听觉，我无法自动验证）**：三个音效的音量与距离衰减手感
（当前全用 `ObjectAudioComponent()` 默认值：volume 1.0、`INVERSE_SQUARED`、reverbVolume 1.0）、
飞行音循环的接缝是否明显、待飞音与飞行音重叠时是否别扭。

## 2026-08-18 — 五条反馈：飞行音门禁 / 骰子音 / 音量 / 飞机朝向 / 骰子随机性；外加 HUD 面板居中

### 1. 待飞时不放飞行音
机库→待飞那一跳同样走 `animateMove`，所以飞行音被无条件启动了。新增
`PlanePieceRenderer.isOnBoard(team, index, state)`（`OnPath && !isInStandby`），
`applyFlightSound` 的 `wanted` 改为 `key in flying && isOnBoard(...)`。

### 2. 骰子音不自然 —— 素材是三段采样，不是一个音效
实测 `dice_throw.mp3` = 5448ms / 24000Hz / 160kbps / -25.2 LUFS，**内含三条 take**：
可听内容 0–831ms（起音 181ms）、2701–3280ms（起音 2789ms）、4783–5343ms（起音 4932ms），中间近乎静音。
而整个投掷只有 1892ms（tumbleAlong 736 + hold 900 + arcBack 256，由 46×16ms 帧推得）。所以旧实现同时犯三个错：
- 起音晚 181ms → 前 165ms 是无声起飞
- **落地那一刻完全没声音**（take 1 在 736ms 已衰到约 -90dBFS，而 736ms 正是骰子触板的瞬间）
- 投掷结束后还会响两次幽灵骰子声（约 +897ms 和 +3040ms），骰子已静止；AI 回合还会叠在飞机飞行音上

修法：两个发声子实体共用同一 `AudioResource`（一个控制器不能同时处在文件两处）。
`TUMBLE_CUE_MS = 168`（帧 7，让起音落在 play 后约 13ms）作抛出音；tumbleAlong 结束时
`tumbleAudio.stop()`（此处约 -90dBFS，无爆音，且顺带保证 take 2 永不触发），同时
`LAND_CUE_MS = 4920`（帧 205，take 3 起音最陡）作落地音，约 420ms，完全落在 900ms 停留内自然收尾。
`finally` 里两个都 stop（取消路径），`detach` 里都 close。

### 3. 飞行音量
实测 `plane_flight.mp3` = 10032ms / -9.4 LUFS / 真峰 **+0.1 dBFS（削顶）**，且响度随移动格数变化：
1 格窗口 -29.5 LUFS，6 格 -8.7，跨度 20.8 LU。对比骰子 -25.2 LUFS，难怪盖住一切。
- `emitterFor` 增加 `volume` 参数 → `ObjectAudioComponent(volume = …)`；`FLIGHT_VOLUME = 0.10f`，
  `STANDBY_VOLUME = 0.45f`（待机音 -8.4 LUFS 也偏热，但是短音）。
- `applyFlightSound` 每次 play 前 `seekTo(FLIGHT_CUE_MS = 3000)`（帧 125）。两个理由：
  避开 clip 的爬升段，让响度不再取决于走了几格；且 SDK **未说明 `stop()` 是否回卷**，
  显式 seek 让这个问题无关紧要。剩余 7032ms > 最坏 6944ms（7 跳 × 992ms），循环只作保险。

### 4. 飞机朝向
- `BoardGeometry.headingDegrees(from, to)` = `atan2(dx, dz)` 度数，0° = +Z，+90° = +X；
  零长腿返回 null（"保持原朝向"）。`settledHeadingDegrees(team, i, piece)` = 刚飞过的那条腿；
  停在停机坪时取"即将飞的那条腿"。用**格心** `positionFor`，不用带偏移的 `targetPosition`。
- **每个机型的机头轴是从 usdz 解出来的**（四个 `scene.usdc` → usda）。四个 stage 都是 Y-up、
  `metersPerUnit = 0.01`。红：螺旋桨关节 z=+11.71、尾关节 z=−3.86；绿：`Prop` z=+4.4、`BackWing` z=−5.7
  → 机头 **+Z**。蓝：`Propeller_M_Plane_0` x=−70.4 对 `BackFlap` x=+142.0；黄同理沿 −X
  → 机头 **−X**。故 `MODEL_YAW_OFFSET_DEG = {RED 0, GREEN 0, BLUE 90, YELLOW 90}`。
- **yaw 不取负。** 这里很容易照抄 `BoardRenderer` 的取负。它必须取负的真正原因是
  `BoardGeometry.rotate(A)` 是俯视顺时针，即 `R_y(−A)`；在 BoardGeometry 自身坐标里测得的朝向
  本来就已在 SDK 的 yaw 语义中。取负会让所有飞机倒着飞。
- 只写 yaw 一个欧拉分量。SDK 按 **extrinsic `M_yaw(Y) * M_pitch(X) * M_roll(Z)`** 合成（math.md:448-460），
  单独 yaw 就是绕世界 +Y 的干净旋转。
- 逐跳插值：`SlideTrack` 增加 `fromYaw`/`toYaw`，`playHop` 用 `lerpAngleDegrees` 同步转向。
  `animateMove` 新增 `from: PieceState` 参数，`legs = listOf(from) + waypoints`，
  否则第 0 跳会沿用**上一条**腿的朝向，在棋盘拐角处最多偏 70.7°。
  **关键**：每跳开头 `tr.fromYaw = tr.toYaw`。若改为读 `piece.yawDegrees`，那是走完全程才写的，
  第 2 跳起每跳都会弹回移动前的朝向 —— 这个 bug 是对抗性复核抓出来的，不是我自己发现的。

### 5. 骰子随机性 —— RNG 是公平的，问题在表现层
实测（用项目自己的 kotlin-stdlib 2.1.20 跑真实表达式 `Random.Default.nextInt(1, 7)`，非重写）：
600 万次每面偏差 ≤0.045pp；10 万次 χ²=10.235（p=0.05 临界 11.07，通过）；
**连续两次相同 = 16.655%，理论 16.667%**。无固定种子、`Random` 不是每次重建、无取模偏差、range 正确。
`VALUE_TO_ROTATION` 六项朝上的面互不相同（5→+Y, 2→−Y, 1→−Z, 6→+Z, 3→+X, 4→−X，
对立面 (1,6)(2,5)(3,4) 均和为 7），**不存在视觉别名**。
真实放大器有三个：摇到 6 可再摇（同一回合内连续两次的第一次**必然**是 6，占 16.35% 的回合）；
开局 83.5% 的点数因无合法走法作废，且 `TurnPassed` 把 HUD 点数清空，玩家看到"转了、没数字、没变化"；
以及**落地姿态从不重置** —— 同一点数每次落地姿态逐像素相同，看起来像"骰子没动"。
已修最后一条：每次投掷抽一个 `restYaw`（`restingPose(value)` 带上它）。
yaw 是 `VALUE_TO_ROTATION` 唯一未用的通道，且绕世界 +Y，**不会改变朝上的面**。
`DiceEngine`/`GameEngine` 一行未改 —— 改它们只会让骰子变得不公平。

### 6. HUD 面板居中
`PANEL_OFFSETS[1]` 从 `Vector3(0.42f, …)` 改为 `Vector3(0f, …)`，与另两个面板一致：
面板是 rig 的子节点，rig 局部 +X 是落座玩家的右手方向，x=0 即玩家中心线，
也就是正对面那条航道的中线。

**测试**：117 → 123 全绿。新增 `HeadingTest`（6 条）覆盖 0°=+Z/+90°=+X 约定、零长腿返回 null、
**所有队所有棋子所有状态都有朝向**、停机坪朝向"即将飞的腿"而非反向、
绕环一圈总转角恰为 360°（能抓出符号错误或坐标混用）、
以及单腿转角上界 —— 实测最陡拐角 **70.7°**（52 对里有 9 对超过 50°），
所以断言用的是 `< 90 && > 65`；写成 ±50 会**假失败**，这个数字也是复核纠正的。

**验证状态**：模拟器已装好启动，音频加载零失败，crash 干净。
**真机中途掉线**（`adb devices` 里已无 `PB311XKGL4160087B`），这一版尚未装上真机。
教训：`adb -s <离线设备>` 会一直 `waiting for device` 挂住 —— 应先用
`adb devices | awk '/\tdevice$/{print $1}'` 过滤在线设备。

**待你确认（听觉/视觉）**：飞行音 0.10 是否合适（可试 0.08/0.12）、待机音 0.45、
骰子抛出与落地两段是否贴合动作、四队飞机是否都机头朝前（蓝/黄用了 +90° 偏移，
若某队侧飞或倒飞，告诉我是哪一队即可）。

## 2026-08-18 — AI 重写：加权评估函数（对旧 AI 胜率 86.3%）

**先建度量，再改 AI。** 新增测试侧无头自对弈：`SelfPlay.play(seed, strategies)` /
`SelfPlay.match(challenger, baseline, seeds)`，驱动**真** `GameEngine`（同 `BoardStage` 的
roll → legalMovesForPendingRoll → applyMove 序列，含 6 点续摇与三连 6 惩罚），只替换骰子为种子化。
每个种子打两遍并交换座位 —— RED 先手且四个机库不等价，不交换的话胜率主要在测座位运气。
`LegacyPlaneAi` 是旧 AI 的逐字冻结副本，作为永久基准对手，**不要"改进"它**。

**instrument 先自证**（`SelfPlayHarnessTest`）：同算法互打必须恰好 50/50 **且完成子数完全相同**
（实测 40–40，419/419 —— 只看胜负是更弱的检查，子数相同才同时证明确定性与座位对称）；
40 局全部自然结束（254–492 次摇骰，中位 352，无一触顶 `ROLL_CAP`）。

**改前的量化基线**：旧 AI vs "取第一个合法走法" 仅 **54.5%** —— 比完全不思考只强 4.5 点。

**一个反直觉的实测发现（救了一次错误设计）**：在 resolve 后的局面上做纯粹"最大化推进"的贪心，
vs 不思考只有 **33.3%**，比什么都不做**更差**。原因是从机库起飞（→待飞）的推进增量为 0，
贪心因此永不起飞；而赢棋需要四架全部到家，且环上无安全格，孤子就是靶子。
教训：这类权重方案不能靠直觉信，必须打过才算。

**旧 AI 的结构性缺陷**（读码）
1. `pathValueOf(it.to)` 排的是 `Move.to`，即**撞色跳(+4)/飞行道(+12) 之前**的落点 ——
   对全局最大的一次性提速完全无感。新版评估 `GameEngine.resolve` 之后的局面，两种跃迁自动计入。
2. 字典序意味着任何吃子压倒一切，哪怕吃空子而放弃自己即将到家的飞机。
3. 只看起点是否危险，不看**落点**。
4. 完全不用僚机叠棋（免疫单机 + 把对手弹回去），也不考虑封路。
5. `isAtRisk` 没算对手的撞色跳/飞行道射程。

**新版**：`score(move) = positionValue(resolve(state, move)) - positionValue(state)`，
**一个单位 = 一格推进**，所有项同尺度，所以"这项是否和那项重复计分"是可回答的而非凭感觉。
核心权重（每项都做过消融实验）：
- `HANGAR_COST = 26.0`（机库飞机记 −26）。**最重要的一个数**，一举替掉另两版手写的五个起飞相关项 ——
  起飞、我方吃子、被吃、三连 6 惩罚全部由同一个减法对称地导出。
  凭第一性原理估的 ~4 会输：设 10 时胜率 38.3%。
- `STANDBY_VALUE = 2.0`、`LANE_SAFETY = 8.0`、`FINISH_BONUS = 20.0`、`WIN_BONUS = 1000`（哨兵非价格）
- `OPPONENT_SHARE = 0.55` × (0.5 + 相对领先度)，在根节点算一次并固定，避免"用被自己改动过的尺子评分"
- `RISK_W = 1.25`、`THREAT_DECAY = 0.85^(还差几家行动)`、威胁按棋子累加并封顶 1.0
- `STACK_BONUS = 8.0`（仅计多回合免疫与同量反吃，避免与 lookahead 重复）、`SELF_BLOCK_W = 0.35`
  （自家叠棋也会挡自家飞机，关掉降到 49.3%）
- `LOOKAHEAD_W = 0.40`（下一轮各面均值最优增益），`DEAD_FACE_PIPS = 3.5`（无合法走法的面按"少摇一次"计价）
- 叠棋一律按 `ringIndexOrNull` 而非原始 path 分组 —— path 键分组已经三次导致"两个待飞算叠棋"的 bug

**已删除的项（建过、测过、删掉）**：`DENIAL_WEIGHT`（我方叠棋弹开对手的收益）中性但每候选多约 150 次
`destinationAfterBlocking` 调用；`LANE_STEP`；`OPPONENT_SETBACK`（与对手权重重复）；
`STACK_TEMPO`（与 lookahead 重复）。中性**且**昂贵才是删除的理由。

**实测（我独立复现，非采信报告）**
| 对局 | 胜率 |
|---|---|
| 新 vs 旧，留出种子 500..649 | **86.3%**（259–41，完成子 1634/968） |
| 新 vs 旧，调参种子 0..149 | 86.0%（258–42） |
| 新 vs 不思考 | 87.5% |
| 旧 vs 不思考 | 54.5% |
| 新 AI 镜像对局 | 恰好 50.0%，子数 335/335 |
调参区与留出区仅差 0.3 点 → 不是拟合种子。

**测试**：126 → 127 全绿。原有 6 条 `PlaneAiTest` **一条没破**。
新增 `the challenger is measurably stronger than the AI it replaced`（种子 500..599，断言 >75% 且到家子数更多；
全程种子化、确定性，不是统计性抽样，不会偶发失败）与
`move choice is deterministic and independent of the order legalMoves arrives in`
（同一局面重复 20 次 + 输入逆序，结果必须一致 —— 打分式选择很容易让实参顺序从 max-by 平局里泄漏进结果）。

**一处诚实的覆盖率损失**：`a move that only shuffles within the same threat's reach gets no escape credit`
仍然通过，但**不再是它注释所写的原因** —— 原注释说"落到 advance-whoever-leads 平局分支"，
现在两步得分本就不同，安全那步是靠实力赢的。已改注释；纯平局分支现由
`otherwise advances whichever piece is furthest along` 覆盖（该处两步得分是精确的 4.0 对 4.0）。

**操作教训（第二次踩）**：`adb -s <设备>` 在设备刚掉线/重连的窗口里仍会 `waiting for device` 挂死，
把一条安装命令拖满 10 分钟。macOS 没有 `timeout`，用
`to() { perl -e 'alarm shift; exec @ARGV' "$@"; }` 给每条 adb/pico-cli 套硬超时。

**验证状态**：真机（`lastUpdateTime=2026-08-18 17:34:50`）与模拟器均已装好，
真机启动干净、音频加载零失败、crash buffer 空。
**待你实际对局感受**：AI 是否明显会抢飞行道、会结僚机、会在落点危险时不贪进。

## 2026-08-18 — 骰子高亮反馈移除；骰子音效改用同一段（take 1）；游戏背景音乐

**去掉骰子高亮反馈**：移除 `DieRenderer` 的发光壳整条链路 —— `glow` 字段、创建代码
（`UnlitMaterial.create(BlendingMode.ADD)` + 球体网格）、`throwTo` 里的启停、`detach` 清理、
`GLOW_RADIUS_FACTOR`/`GLOW_COLOUR` 常量。**放大（`THROW_SCALE=1.7f`）和抛掷弧线保留**——
用户只要求去掉高亮，不是去掉放大。顺带清掉 5 个因此变成死代码的 import
（`ModelEntity`/`BlendingMode`/`MeshResource`/`UnlitMaterial`/`Color4`）。

**骰子音效只用 take 1**：用户先说"只使用前三秒"，后又明确"只要 take 1"——两者一致，
已在同一次改动里满足。重新精测（不用旧数字）`dice_throw.mp3` 的可听边界：
take 1 ≈160–655ms，take 2 ≈2746–3097ms（**跨过 3000ms 边界**，不完全在前三秒内），
take 3 ≈4805–5252ms（完全在三秒之外）。故落地音的取样点从 take 3 改为**复用 take 1**
（`launchAudio`/`landAudio` 两个独立发声实体，都 `seekTo(DICE_CUE_MS=168L)`），
起飞音与落地音是同一段素材的两次独立播放，不再读取文件任何 3 秒之后的内容。
常量从 `TUMBLE_CUE_MS`/`LAND_CUE_MS` 合并为一个 `DICE_CUE_MS`。

**新增游戏背景音乐**：`/Users/zohar/Downloads/ksyburt-cosmic-melody-434980.mp3`
→ `app/src/main/assets/audio/background_music.mp3`（29.6s / 44.1kHz / 立体声 / -14.0 LUFS）。
新类 `AmbientMusicPlayer`（与 `BoardRenderer`/`PlanePieceRenderer`/`DieRenderer` 同款
`attachTo`/`detach` 生命周期），场景 `initial{}` 里 attach、`DisposableEffect` 里 detach。
**用 `AmbientAudioComponent`（`ORIENTATION_ONLY`），不用 `ObjectAudioComponent`** ——
后者音量随听者距离衰减，背景音乐会随玩家凑近棋盘忽大忽小；SDK 文档明确把
`ORIENTATION_ONLY`（其默认值）标注为"纯 VR 背景音乐"的推荐模式，故用默认构造，未发明配置。
`MUSIC_VOLUME = 0.12f`（约 -18.4dB，有效约 -32.4 LUFS），压到骰子音效（有效约 -25.2 LUFS）
和飞行音（有效约 -29.4 LUFS）之下——判断起点，非测量最优解。循环播放，一次启动。

**测试**：127 全绿（本轮无新增/删除测试——纯音频资源与视觉表现层改动，
不新增会通过的假测试来充数）。已装模拟器与真机（`lastUpdateTime=2026-08-18 20:19:02`），
两处 crash buffer 均干净，四个音频资源加载/prepare 全部成功。

**待确认（听觉，我无法自动验证）**：背景音乐音量 0.12 是否合适、循环接缝是否明显、
骰子起飞/落地两次复用同一段素材是否显得单调。

## 2026-08-18 — 修复：骰子抛到棋盘中间时陷进棋盘

**症状**：「骰子旋转放大丢到棋盘中间位置时陷进去了」。

**根因**：`BOARD_CENTRE = Vector3(0f, TARGET_DIE_SIZE_M / 2f, 0f)` 的高度是按骰子**未放大**
（1x 缩放）时的半高算的——未放大时骰子底面恰好贴合 y=0 的棋盘面。但整个投掷期间
（`tumbleAlong` 飞向中心 → `HOLD_AT_CENTRE_MS` 停留 900ms → `arcBack` 飞回)`setScale(THROW_SCALE=1.7f)`
一直生效，`setScale(1f)` 要到 `finally` 里、`arcBack` 返回之后才执行。
所以骰子放大到 1.7 倍时半高变成 `0.025 × 1.7 = 0.0425m`，但落点高度仍是 `0.025m`，
陷进棋盘约 `(1.7−1) × 0.025 = 0.0175m`——且这恰好发生在停留展示点数的那 900ms 里，
正是玩家在看结果的时候。

**修法**：`BOARD_CENTRE` 高度改为 `TARGET_DIE_SIZE_M / 2f * THROW_SCALE`。
确认过 `BOARD_CENTRE` 在全项目只有两处引用（`tumbleAlong` 的终点、`arcBack` 的起点），
都在骰子处于放大状态期间，改这一个常量就完整覆盖，不影响静止时（`restPosition`/待机位置）的高度。

**范围说明**：`tumbleAlong` 起点和 `arcBack` 终点分别对齐 `restPosition`（未放大时的高度），
理论上放大瞬间那一帧也会有同样量级的下陷，但两处都被抛物线 `lift` 迅速抬起
（首帧 `lift ≈ 1.36cm`，已盖过下陷量），肉眼不可见，本次未动。

**测试**：127 全绿（几何常量的手工修正，无新增测试）。已装模拟器与真机验证启动干净、
音频加载零失败、crash buffer 空。

## 2026-08-19 — 归家小道多算了一格 + 补上"叠棋/终点撞墙反弹"规则

**症状一**：用户实测截图数出归家小道（排除外围共享跑道）只有 6 个印刷格，但应用里从
小道口走到终点实际要多于 6 步。

**根因**：`Track.FINISH_PATH = LOOP_LAST_PATH + HOME_LANE_SIZE + 1`——比 6 个归家小道格
多算了一个"第 7 格"。`BoardGeometry.HOME_LANE_PX` 每队确确实实只实测了 6 个坐标
（`slot` 0..5），而旧的 `homePosition(team)` 是从第 6 格坐标"往中心风车图案再偏一点"
凑出来的合成坐标，不对应任何实测的印刷格——这就是那多出来的、看不见的第 7 步。

**修法**：`FINISH_PATH` 改成 `LOOP_LAST_PATH + HOME_LANE_SIZE`（56，不再 +1）。
第 6 格（`homeLanePosition(team, 5)`）本身就是终点：`Track.isInHomeLane` 的"仍在小道上
走"区间因此收窄成 51..55（5 个值），`FINISH_PATH`（56）单独覆盖第 6 格并同时代表
"已完成"，`BoardGeometry.positionFor` 的终点分支直接复用 `homeLanePosition(team,
HOME_LANE_SIZE-1)`，删掉了整个 `homePosition()`/`FINISH_ARM_BIAS`（不再需要合成偏移——
`occupancyKey` 早就按 `"finish:$team"` 分队分组，同队多机重叠自然靠既有的
`coOccupantOffset` 错开，跟风车中心偏移无关）。副作用是从小道口（`path 50`）出发，
最大点数 6 恰好等于 `FINISH_PATH - LOOP_LAST_PATH`，一次最大点数正好能到家——不再需要
先摇完 6 格再额外精确摇 1 点。

**症状二（同一次会话，用户追加反馈）**："当前方叠机时，数量少的一方走棋还能停到叠机格
处。正确处理是走到多棋处前折返剩余格数，该规则同样适用于终点小道上投到的点数大于到达
终点时进行折返。"

**根因**：`GameEngine.destinationAfterBlocking` 原来只检查**途经**格（`step in 1 until
roll`），从未检查落点格本身——注释原文明确写着"落点不算阻挡"。`resolveCellEffects` 的
"少的一方被免疫、双方共存"逻辑因此对*直接摇骰落子*也生效了，等于允许人数更少的一方直接
停在对方更大的叠棋上。终点这一侧，`destinationOrNull`（旧名）对超出 `FINISH_PATH` 的点
数直接判非法（从合法走法里剔除），不会反弹。

**修法（用户确认的公式）**：
- 叠棋撞墙反弹：`destinationAfterBlocking` 新增落点格专用检查
  `biggerOpposingStackAtRing`——只有**对方**叠机数量**大于**己方本次移动的棋子组大小才
  算阻挡（己方自己的叠棋、同等或更小的对方叠机都仍是普通落子，交给
  `resolveCellEffects` 结算，行为不变）。墙位定在叠机格**前一格**（`crossed - 1`，不是
  叠机格本身——飞机根本进不去那一格），耗光到墙的点数后剩余点数原地往回走。这跟"途经
  格"用的墙位（叠机格本身）刻意不同：途经格只是路过，落点格是真的进不去。
- 终点超点撞墙反弹：`destination`（原 `destinationOrNull`，因为现在永远有解所以改成非
  空返回）在未阻挡的直线走法超过 `FINISH_PATH` 时，改成 `2 * FINISH_PATH - unobstructed`
  ——墙位就是终点本身，超出几点就回退几点。数学上只有已经进了小道（`path > 50`）才可能
  超点（`50 + 6 = FINISH_PATH` 恰好打满，跑道上不可能超）。
- `resolveCellEffects` 里"对方叠机更大则免疫、双方共存"那条分支现在只有两条路能到达：
  同等/更小的对方叠机（不阻挡），或撞色跳/飞行的落点（这两个机制自己的"途经格阻挡"检查
  不重新校验落点格，沿用旧行为，本次未动——用户只提了普通摇骰移动）。

**测试**：131 全绿（原 127，新增/改写 4 条：终点第 6 格即终点、超点反弹、叠机落点反弹、
同等/更小叠机仍正常落子、己方叠机绝不阻挡自己）。`assembleDebug` 也过。

**追加修复（用户同一会话内要求）："需要反弹动画从起点滑到墙再倒退"**：原来
`movementWaypoints` 只会沿 `from.value` 到 `to.value` 单调走一条直线（全升或全降），
反弹后的滑动动画会"直接从起点滑到反弹后的终点"，看不出撞墙这一下——而且这从 `Move`
的 `from`/`to` 两个字段本身就**推不出来**墙在哪，doc 注释原文就写着"forward leg... is
not reconstructible from a Move alone"。

**修法**：给 `Move` 加一个可选字段 `bounceWall: Int?`（默认 null，不影响任何现有
`Move(...)` 调用点）——只在真正撞墙反弹时才有值：途经格阻挡时墙是叠机格本身，落点格
阻挡（数量少的一方）时墙是叠机格**前一格**，终点超点时墙是 `FINISH_PATH` 本身。
`GameEngine.legalMoves` 内部把 `destinationAfterBlocking`/`destination` 拆成
`blockedBounce` 返回 `Bounce(wall, landing)`，两处再把 `wall` 一起塞进生成的 `Move`。
`movementWaypoints` 见到 `bounceWall != null` 就先正向走 `from+1..wall`，再反向走
`wall-1 downTo to`，其余情况（`bounceWall == null`）走法不变。

**测试**：132 全绿（新增/改写 4 条：`overshooting the finish...` 改成分别断言
`move.to`/`move.bounceWall`，`MovementWaypointsTest` 里把已经过时的"a bounce back off
a stack walks backwards"换成三条——途经格反弹、落点格反弹（墙在前一格）、终点超点反
弹——各自验证动画确实先走到墙再倒退）。`assembleDebug` 也过。视觉效果本身仍待你在
真机/模拟器上确认。

## 2026-08-19 — 待排查：飞机被吃回机库后模型动画未停止（尚未定位根因）

**症状**：用户反馈"玩家的飞机被吃掉返回飞机库后没有停止模型动画播放"。

**这不是新症状**：2026-08-18 的"回合串行化"条目记录过几乎一样的画面（A 还在飞就被 B
吃回机库，A 的动画还在放），根因是 `isRolling`/`isTurnBusy` 门禁当时只覆盖了 AI 路径，
人类落子路径没有在 `applyMove` 之前同步置位。那次修完后，`BoardStage.kt` 现在两条落子
路径（AI 的 `rollAndAdvance`、人类的 `detectSpatialPointerEvent`）在 `applyMove` 之前都
同步 `isTurnBusy = true`，我复核过——`applyMove` 全项目就这两处调用，两处门禁写法一致，
`animateMove` 的 `flying`/`animating`/`generations` 三个所有权集合在 `finally` 里也都正
确释放，静态读代码没找到第三条能绕开门禁、或者让被吃的飞机动画状态卡住的路径。

**卡在哪**：`animateMove` 头部那行 `TURN OVERLAP` 探针日志正是 2026-08-18 那次修复留下
的兜底探针，专门等这类回合重叠再发生时自己喊出来。静态分析已经到头了——下一步必须是
运行时证据，不能再猜。

**需要你做的**：复现一次这个症状后，跑
`adb -s <设备号> logcat -d -s SpaceFlightChess:E | grep -E "TURN OVERLAP|roll:|walk:|move\["`，
把输出发过来（或者告诉我 `TURN OVERLAP` 有没有出现）。如果它出现了，说明门禁还有没堵上
的洞，需要继续往下查是哪条路径；如果没出现，说明是全新的根因，得从别的角度重新查起
（比如查是不是三连 6 惩罚、僚机整组移动，或者某个还没被覆盖的状态转换路径）。

## 2026-08-19 — 三条新音效 + 红方模型动画"一启动就不再停止"（部分修复，视觉未确认）

**新增三条一次性音效**：`吃子时使用音效`/`玩家胜利结束时使用音效`/`玩家失败时使用音效`。
素材来自 `~/Downloads/射击.mp3`/`胜利.mp3`/`失败.mp3` → 改 ASCII 名存进
`app/src/main/assets/audio/capture_hit.mp3`/`victory.mp3`/`defeat.mp3`。实测响度（ffmpeg
`loudnorm`）：射击/胜利均 -9.7 LUFS，失败 -11.4 LUFS，都比背景音乐（-14.0 LUFS 原始）热。
新类 `EventSoundPlayer`（`AmbientAudioComponent`，跟 `AmbientMusicPlayer` 同样的理由——这
三条是玩家必须听到的事件提示音，不该随头部朝向/距离衰减；每条音效自己一个子 entity，跟
`PlanePieceRenderer.attachSounds`/`DieRenderer` 已有的"一条音效一个 emitter"风格一致）。
挂载点：`BoardStage.publish()` 里 `GameEvent.Moved` 的 `outcome.capturedPieces.isNotEmpty()`
触发吃子音效；`GameEvent.Won` 按 `event.ranking.first() == humanTeam` 分别触发胜利/失败音
效。音量 `CAPTURE_VOLUME=0.35`/`VICTORY_VOLUME=DEFEAT_VOLUME=0.6`——判断起点，非测量最优
解（吃子是高频事件所以压得更低，胜负各只响一次、且响的时候没有其它音效竞争，留得更响）。

**症状二**：用户反馈"红方的飞机一经启动模型动画后不会再停止"。

**已知同类历史**：`stopAnimations` 此前已经修过一次类似 bug（"被吃回机库后动画还在
播"）——根因是 `startAnimations` 在模型的**每一个**持有动画的实体上调用 `playAnimation`
（黄方 23 个），但当时的 stop 只调用了单个 `model.stopAllAnimations()`，漏了三色（黄/蓝/
绿，用节点变换做螺旋桨动画）挂在模型**子孙节点**上的动画。修法是改成遍历
`piece.animations` 逐个实体调 `stopAllAnimations()`。**红方是四个资产里唯一一个用
`SkelAnimation`（骨骼动画）而不是节点变换螺旋桨的**，这次逐实体的修法理论上也覆盖了红
方（红方的骨骼动画资源同样挂在 `piece.animations` 记录的那个实体上，`stopAllAnimations`
是直接对同一个实体调用，不依赖"从父节点级联到子节点"这个未见文档说明的行为）。但用户这
次反馈的症状说明红方仍然没停——查了 `pico-spatial-knowledge` MCP 的
`spatial-sdk_animation_skeletal-animation.md` 和 `com.pico.spatial.core.ecs.animation.md`
两份文档：`AnimationPlayConfig` 的 `transitionMode` 默认值文档明确写着"骨骼动画默认用
CROSSFADE 过渡，其它动画默认用 COMPOSE"——`entity.stopAllAnimations()` 能否正确打断一个
仍在 CROSSFADE 过渡中的骨骼片段，两份文档都没有直接给出答案，这正是红方（骨骼）跟其余三
色（节点变换，COMPOSE）行为可能出现分歧的一个可信切入点，但**没有找到直接证据坐实**。

**已做的修复（有 SDK 文档依据，但未能现场肉眼确认效果）**：`entity.playAnimation(resource)`
的返回值 `AnimationPlaybackController` 此前被整个丢弃——代码只调用了粗粒度的
`entity.stopAllAnimations()`，从没用过 SDK 文档里明确给出的、专门管这一次具体播放的
`AnimationPlaybackController.stop()`。改法：`Piece` 新增 `animationControllers` 字段，
`startAnimations` 保存 `playAnimation` 返回的每个 controller，`stopAnimations` 先对每个
controller 调 `.stop()`（+`.close()`）再执行原有的逐实体 `stopAllAnimations()` 兜底——两
道防线都留着，不是替换。同时加了一条排查专用日志：`stop()` 之后立刻读
`controller.isPlaying()`，如果仍为 `true` 就打一条 `Log.e`，这样以后复现能直接从 logcat
读出"SDK 到底认不认这次 stop"，不用只能靠肉眼看模型转不转来判断。

**验证情况（诚实记录，这次没能看到红方本身）**：装到真机
（`PB311XKGL4160087B`）确认启动无 crash；这台设备上当时已有一局真人（应该是红方，默认
`humanTeam=RED`）在玩的进度，`pico-cli device`/`app` 系列命令能装、能启动、能读
`SpaceFlightChess:E` 日志，但**不能**代替人戴头显做空间面板交互（选阵营面板、摇骰、点棋
子——`shell input tap` 只对 2D 屏幕坐标有效，官方脚本明确写了这条限制），所以红方那几轮
"摇到 6 出待飞 → 再摇一次上跑道"没能由我自己触发。观察到的真实一局里，黄/蓝/绿三色的
"animation stop"日志各出现过一次（黄 23 个 clip、绿 12 个、蓝 7 个），均**没有**触发新加
的"`isPlaying=true` after stop()"警告，说明至少节点变换那三色在这次修复后没有明显异常；
红方本身这次没能等到它自己摇出 6 并上板的时机，日志和肉眼都没有直接证据。**这条需要你戴
上头显亲自测一次**：叫出红方一架飞机进入待飞（摇 6），再摇一次让它落到跑道上，看螺旋桨/
骨骼动作是不是真的停了；如果还没停，麻烦跑
`adb -s PB311XKGL4160087B logcat -d -s SpaceFlightChess:E | grep -E "animation (start|stop)|isPlaying"`
把输出发我——新加的日志会精确报告每次 start/stop 时这颗棋子有几个动画片段、以及 SDK 自己
是否承认 stop 生效了，能把"代码没调 stop"和"SDK 不认 stop"这两种可能直接分开。

**测试**：132 单测全绿（这轮改动都在 Spatial SDK 渲染层，非纯逻辑，没有可测的新单元）。
`assembleDebug` 通过。

## 2026-09-21 — 入口改为平面机库窗口；阵营经 Bundle 传入棋盘 Stage；拆掉 StartPanel 与就座动画

**这是「机库窗口」计划（5 个任务）的最后一步，做完之后的当前状态**：

- **入口容器变了**：`DefaultWindowContainer`（默认空间容器）现在挂的是 `HangarWindow`（平面
  2D 窗口，`content/HangarWindow.kt`），不再是棋盘本身。棋盘 (`BoardStage`) 变成一个非默认
  `Stage(id = BOARD_STAGE_ID)`，只在用户点「开始游戏」时经 `LocalSpatialNavigator.openStage(...)`
  打开（`style = StageStyle.Mixed`，与原来默认 `Stage` 用的沉浸样式一致；`Stage()` 这个 DSL
  函数本身没有 `style` 参数，只能在 `openStage` 调用点给）。`Main.kt` 现在是：
  ```kotlin
  DefaultWindowContainer { PicoTheme { HangarWindow() } }
  Stage(id = BOARD_STAGE_ID) { PicoTheme { BoardStage(bundle) } }
  ```
  两个容器 id（`HANGAR_WINDOW_ID`/`BOARD_STAGE_ID`）、bundle key（`TEAM_BUNDLE_KEY`）和阵营
  解析函数（`teamFromBundleValue`，解析失败退回 `Team.RED`）都在 `content/Containers.kt`。
- **阵营选择搬到机库窗口，棋盘不再自己选**：`HangarWindow` 里四个机位格子（`PlaneTile`）选
  阵营，「开始游戏」调 `openStage(id = BOARD_STAGE_ID, bundle = Bundle().apply { putString(TEAM_BUNDLE_KEY, team.name) })`。
  `BoardStage` 的签名从无参改成 `fun BoardStage(bundle: Bundle?)`，`humanTeam` 直接从这个
  bundle 解析出来，**进 Stage 之前阵营就已经定了**。`StartPanel`/`FactionSwatch`（原来棋盘内
  部的选阵营面板，`content/Panels.kt`）已删除；`BoardStage.kt` 的 `PANEL_IDS`/`PANEL_OFFSETS`
  两个列表按下标一一对应，去掉 `START_PANEL` 那一项时两边都要同步改，改漏一个是不报编译错
  的静默错位 bug（面板挂到错误的偏移上）。
  - `HowtoOverlay`/`HowtoBadge`（"玩法"说明浮层，Task 3 引入）之前是 `internal`，因为
    `StartPanel` 跨文件调用；`StartPanel` 删掉之后两者唯一的调用方就剩 `HangarWindow.kt` 自
    己，已随手降级回 `private`。
  - `StartPanel` 消失的另一个持久后果：棋盘内已经没有任何 UI 会在 `Phase.SETUP` 时调
    `engine.startGame()` 了。`ResultPanel`「再来一局」的 `onPlayAgain` 必须在自己的
    `engine.restart()` 之后**紧接着**自己调一次 `engine.startGame()`——`restart()` 只是把
    phase 打回 `SETUP`，不会自动往下走，少了这一步棋就会卡死在一个没有任何面板能救的
    `SETUP` 状态。这条有专门的回归测试钉住：
    `GameEngineTest.kt` 里 `restart 单独调用会停在 SETUP —— 这就是机库窗口取代 StartPanel
    后必须补 startGame 的原因`。
- **就座不再有过渡动画**：原来 `startWithFaction(team)` 会先把阵营选择结果动画化（棋盘朝向
  + 16 架飞机的机库位置插值过渡 `SEAT_ANIMATION_STEPS=30` 帧），再 `engine.startGame()`——
  这段动画存在的唯一理由是掩盖「棋子瞬移到看起来一样但队伍不对的机库」这个观感问题。现在阵
  营在进 Stage 之前就通过 bundle 定了，`initial` 块里 `BoardGeometry.configureSeat(humanTeam)`
  在 `boardRenderer`/`pieceRenderer` 的 `attachTo` **之前**调用（它们建实体时会读
  `BoardGeometry` 的席位几何），所以整段插值动画、`startWithFaction`、`isSeating` 状态、
  `SEAT_ANIMATION_STEPS`/`SEAT_ANIMATION_FRAME_MS` 两个常量一并删除。
  `AnimationMath.kt` 里的 `lerp`/`smoothStep`/`lerpAngleDegrees` **没有跟着删**——棋子走棋动
  画（`PlanePieceRenderer.animateMove` 经 `DieRenderer` 的滚动效果）还在用。
  - **`engine.startGame()` 反而不能挪到 `configureSeat` 旁边一起提前**——这是 Task 5 review
    抓到的一个真实回归：Compose 的 `LaunchedEffect`（AI 轮询驱动）比 `SpatialView.initial`
    先跑，`startGame()` 一旦在渲染器 `attachTo` 之前就把 phase 翻成 `AWAITING_ROLL`，AI 循
    环会在模型还没加载完、渲染器 `isAttached` 还是 false 的时候就已经摇骰子/走子——这几轮
    全部因为 `isAttached` 门禁静默不播动画，棋盘淡入时已经是走了几步之后的样子，跟被删掉的
    就座动画想掩盖的问题是同一类观感（round-2 logcat 里两次 roll 间隔只有 ~0.7s、没有骰子
    动画，是铁证不是巧合：700ms 的 `AI_THINK_DELAY_MS` 稳定短于 1000ms 的 HMD 等待窗口）。
    现在 `engine.startGame()` 挪到所有 `attachTo` 调用**之后**才执行，`configureSeat` 仍然
    留在最前面——两者不能合并成同一时机。
- **`rotate3D` 对 `SpatialModelView` 内嵌 3D 内容的实测结论**（Task 3 Step 7，机库窗口计划早
  期就验证过、这里是给后续维护者的存档）：`Modifier.rotate3D` **是 Compose 图层变换，不会传
  到 `SpatialModelView`/`SpatialView` 托管的 ECS 内容里**——用一次受控 A/B（round 1 带
  `rotate3D(MODEL_YAW_OFFSET_DEG.getValue(team), RotationAxis3D.Y)`，round 2 把这一行整个删
  掉，其余字节不动）证实：0° 偏移的红方在两轮里像素级不变，说明观察到的模型错位跟 `rotate3D`
  无关。真正病因是四个 `.usdz` 的原始包围盒（bbox）比可见网格大得多、且中心不在网格上——
  `Resizability.FitInside`/等效的手工缩放按包围盒适配容器，包围盒越偏、越虚胖，适配出来的可
  见飞机就越小、越偏。`HangarWindow.kt` 最终没有用 `SpatialModelView` + `rotate3D`，改成了
  `PlaneTile` 里手写 `SpatialView` + ECS（`Entity.loadSuspend` + 量 `getVisualBounds` 自己
  算缩放/回中，见 `content/PlanePieceRenderer.kt` 早就验证过的同一条路径），yaw 直接写实体的
  `TransformComponent`——这才是真的在转模型，不是转面板图层。**以后再有人怀疑 `rotate3D` 转
  不动内嵌 3D 内容，不用重新做这个实验，结论已经定了。**
- **全量测试**：145 个 JUnit 单测全绿（`./gradlew testDebugUnitTest --rerun-tasks`，直接读
  `TEST-*.xml` 而不是只信 Gradle 的 `UP-TO-DATE` 摘要行）；`assembleDebug` 通过。
- **设备闭环验证**（模拟器，`emulator-5554`）：模拟器截图相机不可信判 3D 摆位（这条早就是本
  项目的既定结论），这轮改用 logcat 做证据链——选了**蓝方**（红方是解析失败的退回值，测红方
  测不出 bundle 到底有没有传到），logcat 里 `roll: ... humanTeam=BLUE` 反复出现，证实
  `openStage` 的 bundle 真的传到了 `BoardStage` 并被用于就座，不是退回红方；`openStage(...)
  -> Allowed`、`minimizeWindowContainer(...) -> true`、`restoreWindowContainer(...) -> true`、
  `closeStage() returned` 均出现，返回机库这条出口（后退键 → 退出确认 → 退出）验证通过。因为
  这个模拟器上 `adb shell input tap` 摸不准 `SpatialUI` 组件（两种坐标换算方式都试过，见项目
  记忆 `device-split-real-vs-emulator`），验证过程用了临时代码钩子（`LaunchedEffect` 直接调用
  按钮背后同一个生产函数，如 `selected = Team.BLUE; startGame()`、`returnToHangar()`）代替真
  实点击去触发状态转换，验完已全部移除，不在提交历史里。

**下一步（未做，不是本轮遗漏）**：`ResultPanel`「返回机库」按钮触发的那条 `returnToHangar()`
出口，这轮只做了静态代码核对（`onBackToHangar = ::returnToHangar`，跟退出确认面板调的是同一
个函数引用，且这条路径本身已经被上面那次设备验证跑过一次），没有额外打一整局真人对局去触发
`Phase.GAME_OVER` 现场验证——打满一整局需要走完整 AI 摇骰/落子流程，超出这轮设备锁的合理时
间预算，权衡后没做，如实记录。

**已知后续项（不属于本分支，留给未来分支）**：`ModelNormalization.kt` 的缩放/回中公式只统一了
`PlanePieceRenderer` 和 `HangarWindow` 两处，`DieRenderer.attachTo` 还手写着同一套公式（连
`MIN_MEASURABLE_DIMENSION_M` 都是它自己的一份，跟这边同名不同源）；这次最终审查故意没有动
`DieRenderer` 的行为，只改了 `ModelNormalization.kt` 的 KDoc 别再声称"已全部统一"。
