<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- SDK AAR does not declare this automatically; without it the hand tracking
         provider starts but silently never delivers data (no error). -->
    <uses-permission android:name="com.picovr.permission.HAND_TRACKING" />

    <application
        android:name=".platform.SpatialApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_spatial_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_spatial_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpaceFlightChessTheme"
        tools:targetApi="31"
        android:localeConfig="@xml/locales_config">

        <activity
            android:name=".platform.LaunchActivity"
            android:exported="true"
            android:theme="@style/Theme.SpaceFlightChessTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- DefaultWindowContainer Configuration -->

            <!-- 必填：窗口唯一 id。必须与 content/Containers.kt 的 HANGAR_WINDOW_ID 逐字一致 ——
                 minimizeWindowContainer / restoreWindowContainer 就是按这个字符串定位窗口的，
                 对不上不报错，只会静默什么都不发生。 -->
            <meta-data
                android:name="pico.spatial.windowcontainer.id"
                android:value="SpaceFlightChessHangarWindow" />

            <!-- 窗口形态：'1' = Planar，'2' = Volumetric。
                 选 Planar：机库是一块以 2D 排版为主的选择面板，四架飞机是点缀而非主体。官方文档
                 （spatial-design_foundation_window.md）明确 Planar 可以承载 3D 内容，但深度固定
                 640dp 不可改、超出部分被系统截断——MODEL_BOX_DP 就是按这个预算定的。 -->
            <meta-data
                android:name="pico.spatial.windowcontainer.style"
                android:value="1" />

            <!-- 宽x高，单位 dp。容纳标题 + 四个 180dp 格子（间距 20dp）+ 底部按钮行。
                 Planar 的深度固定 640dp，写不了也不用写。门禁 B 第 1 轮截图后可微调。 -->
            <meta-data
                android:name="pico.spatial.windowcontainer.defaultsize"
                android:value="1040x620" />

            <!-- 关掉系统毛玻璃底板：两个共面半透明层是真机摩尔纹的已知诱因。主窗口的毛玻璃
                 只能在 manifest 关，DSL 侧没有对应参数。 -->
            <meta-data
                android:name="pico.spatial.windowcontainer.materialbackground"
                android:value="0" />

        </activity><!-- Default log level is None — without this the SDK swallows its own errors. -->
        <meta-data android:name="pico.spatial.ui.Logger" android:value="1" />

        <meta-data android:name="icon.3d.list" android:resource="@array/icon_3d_list" />
        <meta-data android:name="icon.sdf.list" android:resource="@array/icon_sdf_list" />
    </application>

</manifest>
